<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

```
<!-- PWA Meta Tags -->
<meta name="theme-color" content="#1e293b">
<meta name="description" content="Professional athlete training dashboard">

<!-- iOS PWA Support -->
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Athlete Dashboard">

<!-- PWA Manifest -->
<link rel="manifest" href="manifest.json">

<title>Athlete Training Dashboard</title>

<!-- Tailwind CSS -->
<script src="https://cdn.tailwindcss.com"></script>

<!-- React -->
<script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
<script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>

<!-- Babel -->
<script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

<style>
    body {
        margin: 0;
        padding: 0;
        overscroll-behavior-y: contain;
        -webkit-overflow-scrolling: touch;
    }
    
    .scrollbar-hide::-webkit-scrollbar {
        display: none;
    }
    .scrollbar-hide {
        -ms-overflow-style: none;
        scrollbar-width: none;
    }
    
    button {
        -webkit-user-select: none;
        user-select: none;
        -webkit-tap-highlight-color: transparent;
    }
</style>
```

</head>
<body>
    <div id="root"></div>

```
<script type="text/babel">
```

import { Plus, TrendingUp, Calendar, Dumbbell, Trash2, Sun, Moon, ChevronDown, ChevronRight, ChevronLeft, Check, X, CalendarDays, Edit, ArrowRight, Play } from ‘lucide-react’;

export default function AthleteDashboard() {
// Exercise emoji mapping - person emojis matching actual exercise movements
const exerciseEmojis = {
// ABC Drills - Running drills with different knee heights
‘H Skip’: ‘🏃‍♂️’,        // High knee running
‘Back Kicking’: ‘🤸‍♂️’,  // Dynamic movement
‘A Skip’: ‘🏃‍♀️’,        // A Drill - knee up, toe up
‘B Skip’: ‘🤾‍♂️’,        // B Drill - knee up, leg extends, pawing motion
‘C Skip’: ‘🤾‍♀️’,        // C Drill - straight leg running
‘Small Jumps’: ‘🤸‍♀️’,   // Jumping movement
‘Leg Raisings’: ‘🧘‍♂️’,  // Controlled leg raise
‘Chest Jumps’: ‘🤾’,      // Explosive jump
‘Thigh Jumps’: ‘🏃’,      // Running jump
‘Pogo Jumps’: ‘⛹️‍♂️’,    // Bouncing jumps
‘Hanuman Jumps’: ‘🤸’,    // Split jump
‘Split Jumps’: ‘🤺’,      // Fencing lunge position
‘Single Leg Hopings’: ‘🕺’, // Dancing/hopping
‘Hop Hop Bound’: ‘💃’,    // Rhythmic movement
‘Duck Walk’: ‘🧎‍♂️’,      // Low squat walk
‘Stretch Walk’: ‘🚶‍♂️’,  // Walking with extension
‘Frog Jumps’: ‘🤸‍♀️’,    // Explosive jump from squat
‘Crocodile Crawling’: ‘🧗‍♂️’, // Low crawling
‘Hand Walk’: ‘🤸‍♂️’,     // Inverted walk
‘Gunny Bag Run’: ‘🏃‍♂️‍➡️’, // Running with resistance
‘High Knee’: ‘🏃‍♀️’,     // High knee running
‘Back High Knee’: ‘🏃‍♂️’, // Backward high knee
‘Back Run’: ‘🚶‍♂️‍➡️’,   // Backward running
‘Boundings’: ‘🤾’,        // Bounding jumps
‘Front Kick’: ‘🥋’,       // Martial arts kick
‘Shapings’: ‘🏃‍♂️’,      // Running form drill
‘Shuffle Run’: ‘🤸’,      // Lateral shuffle

```
// Running - Different running intensities
'2K Run - Time Trial': '🏃‍♂️‍➡️',  // Fast running
'2K Run - Easy Pace': '🚶‍♂️',      // Easy pace
'2K Run - Tempo': '🏃‍♀️‍➡️',       // Tempo pace
'2K Run - Interval Training': '🏃', // Interval sprints

// Weights - Lifting movements
'Good Morning': '🧘‍♂️',        // Hip hinge
'Hamstring Stretch': '🧘‍♀️',  // Stretching
'Clean and Jerk': '🏋️‍♂️',     // Olympic lift
'Snatch': '🏋️‍♀️',             // Olympic snatch
'Clean Snatch': '🏋️‍♀️',       // Olympic lift
'Power Cleans': '🏋️',          // Explosive pull
'Step Up': '🚶‍♂️',             // Stepping up
'Step Ups': '🚶‍♀️',            // Stepping up
'Lunging': '🤺',                // Lunge position
'Lungings': '🤺',               // Lunge position
'Half Squads': '🏋️‍♂️',        // Squat position
'Push Press': '🏋️‍♀️',         // Overhead press
'Plate Action': '🏋️',          // Plate exercise
'Jumping Squads': '🤾‍♂️',     // Jump squat
'Arm Action': '🤸‍♂️',          // Arm swings
'Bench Press': '🏋️‍♂️',        // Pressing
'Shoulder Press': '🏋️‍♀️',     // Overhead press
'Plate Exercises': '🏋️',       // Plate work
'Full Squad': '🧎‍♀️',          // Deep squat

// Multi Gym - Machine exercises
'Lat Pulldown': '🧗‍♂️',      // Pulling down
'Cable Chest Fly': '🏋️‍♀️',  // Chest fly
'Leg Press': '🏋️‍♂️',        // Leg pressing
'Leg Extension': '🧘‍♂️',    // Leg extending
'Leg Curl': '🧘‍♀️',         // Leg curling
'Cable Row': '🚣‍♂️',        // Rowing motion
'Tricep Pushdown': '🏋️',    // Pushing down
'Cable Crossover': '🏋️‍♀️', // Crossing motion

// Plyometrics - Explosive movements
'Box Jumps': '🤾',                              // Jumping onto box
'Medicine Ball Throw - Back Throw': '🤾‍♂️',   // Throwing backward
'Medicine Ball Throw - Front Throw': '🤾‍♀️',  // Throwing forward
'Medicine Ball Throw - Side Throw': '🤸‍♂️',   // Rotating throw
'Medicine Ball Throw - Jump Throw': '⛹️‍♂️',   // Jump and throw
'Medicine Ball Throw - Sitting Throw': '🧘',   // Seated throw
'Medicine Ball Throw - Front Power Throw': '⛹️‍♀️', // Power throw
'Medicine Ball Throw - Stretch Throw': '🤸‍♀️', // Overhead throw

// Hurdles - Jumping over barriers
'Hurdle Hops': '🤾‍♂️',          // Hopping hurdles
'Lateral Hurdle Jumps': '🤸‍♀️', // Side jumps
'Hurdle Walks': '🚶‍♂️',         // Walking over
'Trail Leg Drills': '🏃‍♂️',     // Leg mechanics
'Lead Leg Drills': '🏃‍♀️',      // Lead leg
'Hurdle Step Overs': '🚶‍♀️',   // Stepping over

// Hill Training - Uphill running
'Warm Up - Dynamic Stretching, Stridings': '🧘‍♂️', // Stretching
'80 Meters x 6': '🏃‍♂️‍➡️',     // Hill sprints
'60 Meters x 4': '🏃‍♀️‍➡️',     // Hill sprints
'Wall High Knee': '🏃‍♂️',       // Against wall
'Wall Jumps': '🤾',              // Jumping at wall

// Steps Work Out - Step training
'1 Leg Hoppings': '🤾‍♂️',      // Single leg hops
'Squat Jump 1 Step': '🤸‍♂️',   // Jump squat
'Squat Jump 2 Steps': '🤸‍♀️',  // Jump squat

// Sand Drill - Beach training
'Sand Sprints': '🏃‍♂️',         // Sprinting in sand
'Sand Bounds': '🤾',             // Bounding in sand
'Sand Hill Runs': '🏃‍♀️‍➡️',    // Hill runs
'Sand Agility Drills': '🤸',     // Agility work
'Sand High Knees': '🏃‍♂️',      // High knees

// Wall exercises - Using wall for support
'Wall High Knees': '🏃‍♂️',     // High knees at wall
'Wall Sprints': '🏃‍♀️',        // Sprinting motion
'Wall Drives': '🏃',            // Driving knees
'Wall Push Offs': '🤸‍♂️',     // Pushing off wall

// Step Ups - Stepping movements
'Box Step Ups': '🚶‍♂️',           // Stepping up
'Weighted Step Ups': '🏋️‍♂️',     // With weight
'Alternating Step Ups': '🚶‍♀️',   // Alternating legs
'Explosive Step Ups': '🤾',        // Explosive movement

// Core - Core stability
'Plank': '🧘‍♀️',              // Plank hold
'Side Plank': '🧘‍♂️',         // Side plank
'Russian Twists': '🧘',        // Twisting
'Bicycle Crunches': '🚴‍♂️',   // Cycling motion
'Leg Raises': '🧘‍♀️',         // Raising legs
'Dead Bug': '🧘‍♂️',           // Dead bug position
'Mountain Climbers': '🧗‍♂️',  // Climbing motion
'Ab Wheel': '🧗‍♀️',           // Rolling out
'Hollow Hold': '🧘',           // Hollow position

// Stretching - Flexibility work
'Hamstring': '🧘‍♂️',      // Hamstring stretch
'Quad Stretch': '🧘‍♀️',  // Quad stretch
'Hip Flexor': '🧘',        // Hip flexor
'Calf Stretch': '🧘‍♂️',  // Calf stretch
'IT Band': '🧘‍♀️',       // IT band
'Glute Stretch': '🧘',    // Glute stretch
'Lower Back': '🧘‍♂️',    // Lower back
'Upper Back': '🧘‍♀️',    // Upper back
'Shoulder Stretch': '🧘', // Shoulder
'Chest Stretch': '🧘‍♂️', // Chest

// Cool Down - Recovery
'Jogging': '🏃‍♀️',               // Light jog
'Walking': '🚶',                  // Walking
'Light Stretching': '🧘‍♂️',     // Stretching

// Warm Up exercises
'Dynamic Stretching': '🤸‍♂️',    // Dynamic movements
'Light Jog': '🏃‍♂️',             // Easy jogging
'Arm Circles': '🤸‍♀️',           // Arm rotations
'Leg Swings': '🤸',               // Leg swings
'Hip Openers': '🧘‍♀️',          // Hip mobility
'Stridings': '🏃‍♀️',            // Running strides

// Recovery exercises
'Foam Rolling': '🧘‍♂️',         // Foam rolling
'Static Stretching': '🧘‍♀️',   // Static stretches
'Light Walk': '🚶‍♂️',           // Light walking
'Easy Jog': '🏃‍♀️',             // Easy jogging
'Mobility Work': '🤸‍♂️',        // Mobility exercises
'Cool Down': '🧘',                // Cool down
'Deep Breathing': '🧘‍♂️',       // Breathing exercises
'Recovery Stretching': '🧘‍♀️',  // Recovery stretches

// Core (additional)
'Core': '🧘',                     // Core work

// Cone Drills exercises
'Zig Zag Drills': '🏃‍♂️',        // Zig zag through cones
'Figure 8 Drills': '🏃‍♀️',      // Figure 8 pattern
'T-Drill': '🤾‍♂️',               // T-pattern drill
'L-Drill': '🤾‍♀️',               // L-pattern drill
'Box Drill': '🏃',               // Box pattern
'5-10-5 Shuttle': '🤸‍♂️',      // Shuttle drill

// Circuit Training exercises
'Push Ups': '🤸‍♀️',             // Push ups
'Burpees': '🤾',                 // Burpees
'Jump Squats': '🤸‍♂️',          // Jump squats
'Jumping Jacks': '🤸',           // Jumping jacks
'Lunges': '🏋️',                  // Lunges

// New categories
'Cone Drills': '🏃‍♂️',              // Cone drills
'Circuit Training': '🔄'          // Circuit training
```

};

const exerciseCategoriesWithDefaults = {
‘ABC Drills’: [
{ name: ‘H Skip’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘30 meters distance’, completed: true },
{ name: ‘Back Kicking’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘A Skip’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘B Skip’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘C Skip’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Small Jumps’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Leg Raisings’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Chest Jumps’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Thigh Jumps’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Pogo Jumps’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Hanuman Jumps’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Split Jumps’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Single Leg Hopings’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Hop Hop Bound’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Duck Walk’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Stretch Walk’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Frog Jumps’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Crocodile Crawling’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Hand Walk’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Gunny Bag Run’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘High Knee’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Back High Knee’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Back Run’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Boundings’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘50 meters’, completed: true },
{ name: ‘Front Kick’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘50 meters’, completed: true },
{ name: ‘Shapings’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘50 meters’, completed: true },
{ name: ‘Shuffle Run’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Cross Country 2K Run’: [
{ name: ‘2K Run - Time Trial’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘2K Run - Easy Pace’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘2K Run - Tempo’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘2K Run - Interval Training’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Heavy Weights’: [
{ name: ‘Good Morning’, sets: ‘3’, reps: ‘’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Hamstring Stretch’, sets: ‘3’, reps: ‘’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Clean and Jerk’, sets: ‘12’, reps: ‘’, weight: ‘35’, duration: ‘’, notes: ‘6+4+2 sets, progressive: 35kg+5+2.5’, completed: true },
{ name: ‘Clean Snatch’, sets: ‘12’, reps: ‘’, weight: ‘25’, duration: ‘’, notes: ‘6+4+2 sets’, completed: true },
{ name: ‘Power Cleans’, sets: ‘14’, reps: ‘’, weight: ‘35’, duration: ‘’, notes: ‘8+4+2 sets, progressive: 35kg+5+2.5’, completed: true },
{ name: ‘Step Ups’, sets: ‘3’, reps: ‘65’, weight: ‘30’, duration: ‘’, notes: ‘20+20+25 reps’, completed: true },
{ name: ‘Lungings’, sets: ‘3’, reps: ‘40’, weight: ‘20’, duration: ‘’, notes: ‘40 reps each’, completed: true },
{ name: ‘Half Squads’, sets: ‘3’, reps: ‘24’, weight: ‘60’, duration: ‘’, notes: ‘10+8+6, weight: 50+60+70kg’, completed: true },
{ name: ‘High Knee’, sets: ‘3’, reps: ‘’, weight: ‘20’, duration: ‘30’, notes: ‘30 sec each’, completed: true },
{ name: ‘Push Press’, sets: ‘3’, reps: ‘20’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Stretch Walk’, sets: ‘3’, reps: ‘’, weight: ‘30’, duration: ‘’, notes: ‘30 meters’, completed: true },
{ name: ‘Plate Action’, sets: ‘3’, reps: ‘’, weight: ‘2.5’, duration: ‘’, notes: ‘30 meters’, completed: true },
{ name: ‘Jumping Squads’, sets: ‘3’, reps: ‘16’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Arm Action’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘30’, notes: ‘Double 30 sec’, completed: true },
{ name: ‘Bench Press’, sets: ‘3’, reps: ‘24’, weight: ‘30’, duration: ‘’, notes: ‘10+8+6 reps’, completed: true }
],
‘Light Weights’: [
{ name: ‘Good Morning’, sets: ‘3’, reps: ‘’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Hamstring Stretch’, sets: ‘3’, reps: ‘’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Clean and Jerk’, sets: ‘12’, reps: ‘’, weight: ‘35’, duration: ‘’, notes: ‘6+4+2 sets, progressive’, completed: true },
{ name: ‘Clean Snatch’, sets: ‘12’, reps: ‘’, weight: ‘25’, duration: ‘’, notes: ‘6+4+2 sets’, completed: true },
{ name: ‘Power Cleans’, sets: ‘14’, reps: ‘’, weight: ‘35’, duration: ‘’, notes: ‘8+4+2 sets, progressive’, completed: true },
{ name: ‘Step Ups’, sets: ‘3’, reps: ‘65’, weight: ‘30’, duration: ‘’, notes: ‘20+20+25 reps’, completed: true },
{ name: ‘Lungings’, sets: ‘3’, reps: ‘40’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Half Squads’, sets: ‘3’, reps: ‘24’, weight: ‘60’, duration: ‘’, notes: ‘10+8+6 reps’, completed: true },
{ name: ‘High Knee’, sets: ‘3’, reps: ‘’, weight: ‘20’, duration: ‘30’, notes: ‘30 sec each’, completed: true },
{ name: ‘Push Press’, sets: ‘3’, reps: ‘20’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Stretch Walk’, sets: ‘3’, reps: ‘’, weight: ‘30’, duration: ‘’, notes: ‘30 meters’, completed: true },
{ name: ‘Plate Action’, sets: ‘3’, reps: ‘’, weight: ‘2.5’, duration: ‘’, notes: ‘30 meters’, completed: true },
{ name: ‘Jumping Squads’, sets: ‘3’, reps: ‘16’, weight: ‘20’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Arm Action’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘30’, notes: ‘Double 30 sec’, completed: true },
{ name: ‘Bench Press’, sets: ‘3’, reps: ‘24’, weight: ‘30’, duration: ‘’, notes: ‘10+8+6 reps’, completed: true },
{ name: ‘Shoulder Press’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Plate Exercises’, sets: ‘3’, reps: ‘15’, weight: ‘5’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Full Squad’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Multi Gym’: [
{ name: ‘Clean and Jerk’, sets: ‘2’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: false },
{ name: ‘Snatch’, sets: ‘2’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: false },
{ name: ‘Half Squads’, sets: ‘2’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: false },
{ name: ‘Step Up’, sets: ‘2’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: false },
{ name: ‘Lunging’, sets: ‘2’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: false },
{ name: ‘Lat Pulldown’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Cable Chest Fly’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Leg Press’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Leg Extension’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Leg Curl’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Cable Row’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Tricep Pushdown’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Cable Crossover’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Plyometrics’: [
{ name: ‘Box Jumps’, sets: ‘4’, reps: ‘5’, weight: ‘’, duration: ‘’, notes: ‘5 repetitions, 1 set x 4’, completed: true },
{ name: ‘Medicine Ball Throw - Back Throw’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Medicine Ball Throw - Front Throw’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Medicine Ball Throw - Side Throw’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Medicine Ball Throw - Jump Throw’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Medicine Ball Throw - Sitting Throw’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Medicine Ball Throw - Front Power Throw’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Medicine Ball Throw - Stretch Throw’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Hurdles’: [
{ name: ‘Hurdle Hops’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Lateral Hurdle Jumps’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Hurdle Walks’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Trail Leg Drills’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Lead Leg Drills’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Hurdle Step Overs’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Hill Training’: [
{ name: ‘Warm Up - Dynamic Stretching, Stridings’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘10’, notes: ‘’, completed: true },
{ name: ‘80 Meters x 6’, sets: ‘6’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘60% effort’, completed: true },
{ name: ‘60 Meters x 4’, sets: ‘4’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘60% effort’, completed: true },
{ name: ‘Wall High Knee’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Wall Jumps’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Core’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Steps Work Out’: [
{ name: ‘High Knee’, sets: ‘3’, reps: ‘5’, weight: ‘’, duration: ‘’, notes: ‘3 times’, completed: true },
{ name: ‘Pogo Jump’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘3 times’, completed: true },
{ name: ‘1 Leg Hoppings’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘3 times’, completed: true },
{ name: ‘Squat Jump 1 Step’, sets: ‘2’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘2 times’, completed: true },
{ name: ‘Squat Jump 2 Steps’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘3 times’, completed: true },
{ name: ‘Duck Walk’, sets: ‘2’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘2 times’, completed: true },
{ name: ‘Stretch Walk’, sets: ‘2’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘2 times’, completed: true },
{ name: ‘High Knee’, sets: ‘5’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘5 times’, completed: true }
],
‘Sand Drill’: [
{ name: ‘Sand Sprints’, sets: ‘5’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘50 meters’, completed: true },
{ name: ‘Sand Bounds’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Sand Hill Runs’, sets: ‘5’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Sand Agility Drills’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Sand High Knees’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Wall High Knee’: [
{ name: ‘Wall High Knees’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Wall Sprints’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Wall Drives’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Wall Push Offs’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Step Ups’: [
{ name: ‘Box Step Ups’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Weighted Step Ups’, sets: ‘3’, reps: ‘65’, weight: ‘30’, duration: ‘’, notes: ‘20+20+25’, completed: true },
{ name: ‘Alternating Step Ups’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Explosive Step Ups’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Core’: [
{ name: ‘Plank’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘60’, notes: ‘60 seconds’, completed: true },
{ name: ‘Side Plank’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘45’, notes: ‘Each side’, completed: true },
{ name: ‘Russian Twists’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Bicycle Crunches’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Leg Raises’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Dead Bug’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Mountain Climbers’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘V-Ups’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Warm Up’: [
{ name: ‘Dynamic Stretching’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘5’, notes: ‘’, completed: true },
{ name: ‘Light Jog’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘10’, notes: ‘’, completed: true },
{ name: ‘Arm Circles’, sets: ‘2’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Leg Swings’, sets: ‘2’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘Each leg’, completed: true },
{ name: ‘Hip Openers’, sets: ‘2’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Stridings’, sets: ‘6’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Recovery’: [
{ name: ‘Foam Rolling’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘10’, notes: ‘’, completed: true },
{ name: ‘Static Stretching’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘15’, notes: ‘’, completed: true },
{ name: ‘Light Walk’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘10’, notes: ‘’, completed: true },
{ name: ‘Easy Jog’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘15’, notes: ‘10-15 mins’, completed: true },
{ name: ‘Mobility Work’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘10’, notes: ‘’, completed: true },
{ name: ‘Cool Down’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘10’, notes: ‘’, completed: true }
],
‘Cool Down’: [
{ name: ‘Easy Jog’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘10’, notes: ‘’, completed: true },
{ name: ‘Walking’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘5’, notes: ‘’, completed: true },
{ name: ‘Static Stretching’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘15’, notes: ‘’, completed: true },
{ name: ‘Deep Breathing’, sets: ‘1’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Recovery Stretching’, sets: ‘1’, reps: ‘’, weight: ‘’, duration: ‘10’, notes: ‘’, completed: true }
],
‘Cone Drills’: [
{ name: ‘Zig Zag Drills’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Figure 8 Drills’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘T-Drill’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘L-Drill’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Box Drill’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘5-10-5 Shuttle’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true }
],
‘Circuit Training’: [
{ name: ‘Push Ups’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Burpees’, sets: ‘3’, reps: ‘10’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Jump Squats’, sets: ‘3’, reps: ‘15’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Mountain Climbers’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘High Knees’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘30’, notes: ‘seconds’, completed: true },
{ name: ‘Plank’, sets: ‘3’, reps: ‘’, weight: ‘’, duration: ‘60’, notes: ‘seconds’, completed: true },
{ name: ‘Jumping Jacks’, sets: ‘3’, reps: ‘20’, weight: ‘’, duration: ‘’, notes: ‘’, completed: true },
{ name: ‘Lunges’, sets: ‘3’, reps: ‘12’, weight: ‘’, duration: ‘’, notes: ‘each leg’, completed: true }
]
};

// Category Groups - organize all 18 categories into logical groups based on training plan
const categoryGroups = {
‘Agility & Mobility’: [‘ABC Drills’, ‘Cone Drills’, ‘Circuit Training’],
‘Every Day Routine’: [‘Warm Up’, ‘Step Ups’, ‘Wall High Knee’, ‘Core’, ‘Cool Down’],
‘Weight Training’: [‘Heavy Weights’, ‘Light Weights’, ‘Multi Gym’, ‘Circuit Training’],
‘Explosive’: [‘Plyometrics’, ‘Hurdles’],
‘Endurance’: [‘Cross Country 2K Run’, ‘Steps Work Out’, ‘Hill Training’, ‘Sand Drill’],
‘Recovery’: [‘Recovery’]
};

const [sessions, setSessions] = useState({ morning: [], evening: [] });
const [todaysPlan, setTodaysPlan] = useState({ morning: [], evening: [] });
const [selectedDate, setSelectedDate] = useState(new Date().toISOString().split(‘T’)[0]);
const [expandedCategories, setExpandedCategories] = useState({});
const [showWeekPlanner, setShowWeekPlanner] = useState(false);
const [weeklyPlan, setWeeklyPlan] = useState({});
const [planningDate, setPlanningDate] = useState(’’);
const [planningSession, setPlanningSession] = useState(‘morning’);
const [planningCategories, setPlanningCategories] = useState({ morning: [], evening: [] });
const [currentPlanCategory, setCurrentPlanCategory] = useState(’’);
const [currentPlanExercises, setCurrentPlanExercises] = useState([]);
const [planningStep, setPlanningStep] = useState(‘groups’); // ‘groups’, ‘categories’, ‘exercises’
const [selectedGroup, setSelectedGroup] = useState(’’);
const [planningMode, setPlanningMode] = useState(‘day’);
const [weeklyPlans, setWeeklyPlans] = useState({});
const [selectedWeekDay, setSelectedWeekDay] = useState(’’);
const [showSummary, setShowSummary] = useState(false);
const [showPlanModal, setShowPlanModal] = useState(false);
const [showEditPlanModal, setShowEditPlanModal] = useState(false);
const [showWeekPlanModal, setShowWeekPlanModal] = useState(false);
const [showWorkoutMode, setShowWorkoutMode] = useState(false);
const [showCoachPlanModal, setShowCoachPlanModal] = useState(false);
const [showExportModal, setShowExportModal] = useState(false);
const [showImportModal, setShowImportModal] = useState(false);
const [showSummaryModal, setShowSummaryModal] = useState(false);
const [summarySession, setSummarySession] = useState(‘morning’); // ‘morning’ or ‘evening’ for summary view
const [exportData, setExportData] = useState(’’);
const [importData, setImportData] = useState(’’);
const [coachPlanStep, setCoachPlanStep] = useState(‘workflow’); // ‘workflow’, ‘exercises’, ‘categories’
const [currentCoachSection, setCurrentCoachSection] = useState(’’); // which section is being edited
const [coachPlanSession, setCoachPlanSession] = useState(‘morning’); // ‘morning’ or ‘evening’
const [coachPlanData, setCoachPlanData] = useState({ morning: [], evening: [] });
const [workoutSession, setWorkoutSession] = useState(’’); // ‘morning’ or ‘evening’
const [currentExerciseIndex, setCurrentExerciseIndex] = useState(0);
const [editingSession, setEditingSession] = useState(’’); // ‘morning’ or ‘evening’
const [planningForDate, setPlanningForDate] = useState(’’); // for week planning
const [seasonMonths, setSeasonMonths] = useState(3); // Default 3 months for Season Best

useEffect(() => {
loadSessions();
loadWeeklyPlan();
loadTodaysPlan();
}, [selectedDate]);

const loadSessions = () => {
try {
const key = `athlete-sessions-${selectedDate}`;
const saved = localStorage.getItem(key);
if (saved) {
setSessions(JSON.parse(saved));
} else {
setSessions({ morning: [], evening: [] });
}
} catch (error) {
setSessions({ morning: [], evening: [] });
}
};

const loadWeeklyPlan = () => {
try {
const saved = localStorage.getItem(‘athlete-weekly-plan’);
if (saved) {
setWeeklyPlan(JSON.parse(saved));
}
} catch (error) {
setWeeklyPlan({});
}
};

const saveWeeklyPlan = (plan) => {
try {
localStorage.setItem(‘athlete-weekly-plan’, JSON.stringify(plan));
setWeeklyPlan(plan);
} catch (error) {
console.error(‘Failed to save weekly plan:’, error);
}
};

const saveSessions = (updatedSessions) => {
try {
const key = `athlete-sessions-${selectedDate}`;
localStorage.setItem(key, JSON.stringify(updatedSessions));
setSessions(updatedSessions);
} catch (error) {
console.error(‘Save failed:’, error);
}
};

const saveTodaysPlan = (updatedPlan) => {
try {
const key = `athlete-todays-plan-${selectedDate}`;
localStorage.setItem(key, JSON.stringify(updatedPlan));
setTodaysPlan(updatedPlan);
} catch (error) {
console.error(‘Save plan failed:’, error);
}
};

const loadTodaysPlan = () => {
try {
const key = `athlete-todays-plan-${selectedDate}`;
const saved = localStorage.getItem(key);
if (saved) {
const plan = JSON.parse(saved);
setTodaysPlan(plan);
} else {
setTodaysPlan({ morning: [], evening: [] });
}
} catch (error) {
console.error(‘Load plan failed:’, error);
setTodaysPlan({ morning: [], evening: [] });
}
};

const getPlannedCategoriesForDate = (date) => {
return weeklyPlan[date] || { morning: [], evening: [] };
};

const getNext7Days = () => {
const days = [];
const today = new Date();
for (let i = 0; i < 7; i++) {
const date = new Date(today);
date.setDate(today.getDate() + i);
days.push(date.toISOString().split(‘T’)[0]);
}
return days;
};

const getDayHasPlan = (date) => {
try {
const key = `athlete-todays-plan-${date}`;
const saved = localStorage.getItem(key);
if (saved) {
const plan = JSON.parse(saved);
return plan.morning?.length > 0 || plan.evening?.length > 0;
}
return false;
} catch {
return false;
}
};

// Calculate Personal Best (PB) and Season Best (SB) for an exercise
const calculateBests = (exerciseName) => {
let personalBest = 0;
let seasonBest = 0;

```
// Get all dates from localStorage
const allDates = [];
for (let i = 0; i < localStorage.length; i++) {
  const key = localStorage.key(i);
  if (key && key.startsWith('athlete-sessions-')) {
    const date = key.replace('athlete-sessions-', '');
    allDates.push(date);
  }
}

// Calculate date cutoff for season (default 3 months ago)
const today = new Date();
const seasonCutoff = new Date(today);
seasonCutoff.setMonth(today.getMonth() - seasonMonths);
const seasonCutoffStr = seasonCutoff.toISOString().split('T')[0];

// Go through all historical sessions
allDates.forEach(date => {
  try {
    const sessionData = localStorage.getItem(`athlete-sessions-${date}`);
    if (sessionData) {
      const sessionsData = JSON.parse(sessionData);
      
      // Check both morning and evening
      ['morning', 'evening'].forEach(sessionType => {
        sessionsData[sessionType]?.forEach(category => {
          category.exercises?.forEach(exercise => {
            if (exercise.name === exerciseName) {
              let maxWeight = 0;
              
              // Handle Half Squads with setData
              if (exercise.setData && Array.isArray(exercise.setData)) {
                exercise.setData.forEach(set => {
                  const weight = parseInt(set.weight) || 0;
                  if (weight > maxWeight) maxWeight = weight;
                });
              } else {
                // Regular weight field
                maxWeight = parseInt(exercise.weight) || 0;
              }
              
              // Update Personal Best (all time)
              if (maxWeight > personalBest) {
                personalBest = maxWeight;
              }
              
              // Update Season Best (within season months)
              if (date >= seasonCutoffStr && maxWeight > seasonBest) {
                seasonBest = maxWeight;
              }
            }
          });
        });
      });
    }
  } catch (error) {
    console.error('Error parsing session:', error);
  }
});

return { personalBest, seasonBest };
```

};

const getAllExercisesInSession = (sessionType) => {
const allExercises = [];
sessions[sessionType]?.forEach((category, catIdx) => {
category.exercises?.forEach((exercise, exIdx) => {
allExercises.push({
…exercise,
categoryName: category.category,
categoryId: category.id,
categoryIndex: catIdx,
exerciseIndex: exIdx
});
});
});
return allExercises;
};

const getFilteredExercises = (category) => {
const plannedCategories = getPlannedCategoriesForDate(selectedDate);
const allCats = […(plannedCategories.morning || []), …(plannedCategories.evening || [])];
const plannedCategory = allCats.find(c => c.category === category);

```
if (plannedCategory && plannedCategory.selectedExercises) {
  const allExercises = exerciseCategoriesWithDefaults[category];
  return allExercises.filter(ex => plannedCategory.selectedExercises.includes(ex.name));
}

return exerciseCategoriesWithDefaults[category];
```

};

const toggleCategoryExpanded = (categoryId) => {
setExpandedCategories(prev => ({
…prev,
[categoryId]: !prev[categoryId]
}));
};

const toggleExerciseCompleted = (sessionType, categoryId, exerciseIndex) => {
const updatedSessions = { …sessions };
const categoryIndex = updatedSessions[sessionType].findIndex(cat => cat.id === categoryId);
if (categoryIndex !== -1) {
updatedSessions[sessionType][categoryIndex].exercises[exerciseIndex].completed =
!updatedSessions[sessionType][categoryIndex].exercises[exerciseIndex].completed;
saveSessions(updatedSessions);
}
};

const deleteCategory = (sessionType, categoryId) => {
const updatedSessions = {
…sessions,
[sessionType]: sessions[sessionType].filter(cat => cat.id !== categoryId)
};
saveSessions(updatedSessions);
};

const loadSessionPlan = (sessionType) => {
if (todaysPlan[sessionType]?.length > 0) {
const updatedSessions = {
…sessions,
[sessionType]: […todaysPlan[sessionType]]
};
setSessions(updatedSessions);
saveSessions(updatedSessions);
} else {
alert(`No plan created for ${sessionType} session. Click "Plan Today" to create one.`);
}
};

const getTotalExercises = () => {
const morningCount = sessions.morning?.reduce((sum, cat) => sum + cat.exercises.length, 0) || 0;
const eveningCount = sessions.evening?.reduce((sum, cat) => sum + cat.exercises.length, 0) || 0;
return morningCount + eveningCount;
};

const getCompletedExercises = () => {
const morningCompleted = sessions.morning?.reduce((sum, cat) =>
sum + cat.exercises.filter(ex => ex.completed).length, 0) || 0;
const eveningCompleted = sessions.evening?.reduce((sum, cat) =>
sum + cat.exercises.filter(ex => ex.completed).length, 0) || 0;
return morningCompleted + eveningCompleted;
};

const startPlanningForDate = (date, mode = ‘day’) => {
setPlanningDate(date);
setPlanningMode(mode);

```
if (mode === 'week') {
  // Initialize weekly plans with existing plans for each day
  const days = getNext7Days();
  const initialWeeklyPlans = {};
  days.forEach(day => {
    initialWeeklyPlans[day] = weeklyPlan[day] || { morning: [], evening: [] };
  });
  setWeeklyPlans(initialWeeklyPlans);
  setSelectedWeekDay(days[0]); // Start with first day
  setPlanningCategories(initialWeeklyPlans[days[0]]);
} else {
  const existingPlan = weeklyPlan[date] || { morning: [], evening: [] };
  setPlanningCategories(JSON.parse(JSON.stringify(existingPlan)));
}

setPlanningSession('morning');
setPlanningStep('list');
setShowWeekPlanner(true);
```

};

const startAddingCategory = () => {
setCurrentPlanCategory(’’);
setCurrentPlanExercises([]);
setPlanningStep(‘selectCategory’);
};

const selectWeekDay = (day) => {
// Save current day’s plan before switching
if (planningMode === ‘week’ && selectedWeekDay) {
const updated = { …weeklyPlans };
updated[selectedWeekDay] = planningCategories;
setWeeklyPlans(updated);
}

```
// Load the new day's plan
setSelectedWeekDay(day);
setPlanningCategories(JSON.parse(JSON.stringify(weeklyPlans[day] || { morning: [], evening: [] })));
setPlanningSession('morning');
```

};

const handlePlanCategorySelect = (category) => {
setCurrentPlanCategory(category);
setCurrentPlanExercises(exerciseCategoriesWithDefaults[category].map(ex => ex.name));
setPlanningStep(‘selectExercises’);
};

const toggleExerciseInCurrentPlan = (exerciseName) => {
if (currentPlanExercises.includes(exerciseName)) {
setCurrentPlanExercises(currentPlanExercises.filter(e => e !== exerciseName));
} else {
setCurrentPlanExercises([…currentPlanExercises, exerciseName]);
}
};

const saveCategoryToPlan = () => {
const newCategory = {
category: currentPlanCategory,
selectedExercises: currentPlanExercises
};
const updated = { …planningCategories };
updated[planningSession] = […(updated[planningSession] || []), newCategory];
setPlanningCategories(updated);

```
// Also update weeklyPlans if in week mode
if (planningMode === 'week' && selectedWeekDay) {
  const updatedWeekly = { ...weeklyPlans };
  updatedWeekly[selectedWeekDay] = updated;
  setWeeklyPlans(updatedWeekly);
}

setPlanningStep('list');
setCurrentPlanCategory('');
setCurrentPlanExercises([]);
```

};

const removeCategoryFromPlan = (session, index) => {
const updated = { …planningCategories };
updated[session] = (updated[session] || []).filter((_, i) => i !== index);
setPlanningCategories(updated);

```
// Also update weeklyPlans if in week mode
if (planningMode === 'week' && selectedWeekDay) {
  const updatedWeekly = { ...weeklyPlans };
  updatedWeekly[selectedWeekDay] = updated;
  setWeeklyPlans(updatedWeekly);
}
```

};

const savePlanForDate = () => {
const updatedPlan = { …weeklyPlan, [planningDate]: planningCategories };
saveWeeklyPlan(updatedPlan);
setShowWeekPlanner(false);
setPlanningDate(’’);
setPlanningCategories({ morning: [], evening: [] });
setPlanningStep(‘list’);
setPlanningMode(‘day’);
};

const saveWeeklyPlans = () => {
// Save current day’s changes first
const finalWeeklyPlans = { …weeklyPlans };
if (selectedWeekDay) {
finalWeeklyPlans[selectedWeekDay] = planningCategories;
}

```
// Save all days to weeklyPlan
const updatedPlan = { ...weeklyPlan };
Object.keys(finalWeeklyPlans).forEach(day => {
  updatedPlan[day] = finalWeeklyPlans[day];
});

saveWeeklyPlan(updatedPlan);
setShowWeekPlanner(false);
setPlanningDate('');
setPlanningCategories({ morning: [], evening: [] });
setWeeklyPlans({});
setSelectedWeekDay('');
setPlanningStep('list');
setPlanningMode('day');
```

};

const closePlanner = () => {
setShowWeekPlanner(false);
setPlanningDate(’’);
setPlanningCategories({ morning: [], evening: [] });
setWeeklyPlans({});
setSelectedWeekDay(’’);
setCurrentPlanCategory(’’);
setCurrentPlanExercises([]);
setPlanningStep(‘list’);
setPlanningMode(‘day’);
};

const getCompletedExercisesForDate = (date) => {
try {
const key = `athlete-sessions-${date}`;
const saved = localStorage.getItem(key);
if (saved) {
const dateSessions = JSON.parse(saved);
const completed = { morning: [], evening: [] };

```
    ['morning', 'evening'].forEach(session => {
      if (dateSessions[session]) {
        dateSessions[session].forEach(cat => {
          const completedExs = cat.exercises.filter(ex => ex.completed);
          if (completedExs.length > 0) {
            completed[session].push({
              category: cat.category,
              exercises: completedExs
            });
          }
        });
      }
    });
    
    return completed;
  }
} catch (error) {
  console.error('Error loading completed exercises:', error);
}
return { morning: [], evening: [] };
```

};

const getWeeklySummary = () => {
const days = [];
const today = new Date();
for (let i = 6; i >= 0; i–) {
const date = new Date(today);
date.setDate(today.getDate() - i);
const dateStr = date.toISOString().split(‘T’)[0];
const completed = getCompletedExercisesForDate(dateStr);
const hasCompleted = completed.morning.length > 0 || completed.evening.length > 0;

```
  if (hasCompleted) {
    days.push({
      date: dateStr,
      dayName: date.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' }),
      completed: completed
    });
  }
}
return days;
```

};

const SessionCard = ({ sessionType, sessionData, icon: Icon }) => (
<div className="bg-slate-800 rounded-xl p-4 shadow-xl border border-slate-700">
<div className="flex items-center justify-between mb-3">
<h2 className="text-xl font-bold text-white flex items-center gap-2">
<Icon size={24} className={sessionType === ‘morning’ ? ‘text-yellow-400’ : ‘text-blue-400’} />
{sessionType === ‘morning’ ? ‘Morning Session’ : ‘Evening Session’}
<span className="text-sm text-slate-400">({sessionData?.length || 0} categories)</span>
</h2>
<button
onClick={() => loadSessionPlan(sessionType)}
className=“px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition flex items-center gap-2”
>
<ArrowRight size={18} />
Load Plan
</button>
</div>
{!sessionData || sessionData.length === 0 ? (
<div className="bg-slate-700/50 rounded-lg p-6 text-center">
<p className="text-slate-400">No categories loaded for this session</p>
<p className="text-slate-500 text-sm mt-2">Click “Load Plan” to load planned categories</p>
</div>
) : (
<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3">
{sessionData.map((categoryData) => {
const isExpanded = expandedCategories[categoryData.id];
const completedCount = categoryData.exercises.filter(ex => ex.completed).length;
const totalCount = categoryData.exercises.length;
return (
<div key={categoryData.id} className="bg-slate-700 rounded-lg overflow-hidden">
<div className="flex flex-col p-3">
<div className="flex items-start justify-between mb-2">
<button
onClick={() => toggleCategoryExpanded(categoryData.id)}
className=“flex items-center gap-2 text-blue-400 hover:text-blue-300 transition flex-1”
>
{isExpanded ? <ChevronDown size={20} /> : <ChevronRight size={20} />}
<h3 className="text-base font-bold text-white">{categoryData.category}</h3>
</button>
<button
onClick={() => deleteCategory(sessionType, categoryData.id)}
className=“p-1.5 text-red-400 hover:bg-red-900/30 rounded-lg transition”
title=“Delete category”
>
<Trash2 size={14} />
</button>
</div>
<p className="text-slate-400 text-xs">
{completedCount}/{totalCount} completed
</p>
</div>

```
            {isExpanded && (
              <div className="px-3 pb-3 space-y-2">
                {categoryData.exercises.map((exercise, idx) => (
                  <div key={idx} className={`rounded p-2 ${exercise.completed ? 'bg-slate-600/50' : 'bg-slate-600/20 opacity-60'}`}>
                    <div className="flex items-start justify-between mb-1">
                      <div className="text-white font-semibold text-sm flex-1 flex items-center gap-1.5">
                        <span>{exerciseEmojis[exercise.name] || '💪'}</span>
                        <span>{exercise.name}</span>
                      </div>
                      <button
                        onClick={() => toggleExerciseCompleted(sessionType, categoryData.id, idx)}
                        className={`ml-2 p-1 rounded transition ${
                          exercise.completed 
                            ? 'bg-green-500 text-white hover:bg-green-600' 
                            : 'bg-slate-500 text-slate-300 hover:bg-slate-400'
                        }`}
                        title={exercise.completed ? 'Mark as not done' : 'Mark as done'}
                      >
                        {exercise.completed ? <Check size={14} /> : <X size={14} />}
                      </button>
                    </div>
                    <div className="grid grid-cols-2 gap-1 text-xs">
                      {exercise.sets && (
                        <div>
                          <span className="text-slate-400">Sets:</span>
                          <span className="text-white ml-1">{exercise.sets}</span>
                        </div>
                      )}
                      {exercise.reps && (
                        <div>
                          <span className="text-slate-400">Reps:</span>
                          <span className="text-white ml-1">{exercise.reps}</span>
                        </div>
                      )}
                      {exercise.weight && (
                        <div>
                          <span className="text-slate-400">Weight:</span>
                          <span className="text-white ml-1">{exercise.weight}kg</span>
                        </div>
                      )}
                      {exercise.duration && (
                        <div>
                          <span className="text-slate-400">Duration:</span>
                          <span className="text-white ml-1">{exercise.duration}min</span>
                        </div>
                      )}
                    </div>
                    {exercise.notes && (
                      <div className="mt-1 text-xs text-slate-400">
                        Note: {exercise.notes}
                      </div>
                    )}
                  </div>
                ))}
              </div>
            )}
          </div>
        );
      })}
    </div>
  )}
  {sessionData && sessionData.length > 0 && (
    <div className="mt-4">
      <button
        onClick={() => {
          setWorkoutSession(sessionType);
          setCurrentExerciseIndex(0);
          setShowWorkoutMode(true);
        }}
        className="w-full px-6 py-3 bg-gradient-to-r from-green-600 to-emerald-600 text-white rounded-lg hover:from-green-700 hover:to-emerald-700 transition font-semibold text-lg flex items-center justify-center gap-2"
      >
        <Play size={24} />
        Start Workout
      </button>
    </div>
  )}
</div>
```

);

return (
<div className="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 p-4">
<div className="max-w-7xl mx-auto">
<div className="bg-gradient-to-r from-blue-600 to-purple-600 rounded-2xl p-6 mb-4 shadow-2xl">
<h1 className="text-3xl font-bold text-white mb-2 flex items-center gap-3">
<Dumbbell size={36} />
Athlete Training Dashboard
</h1>
</div>

```
    <div className="bg-slate-800 rounded-xl p-4 mb-4">
      <div className="flex flex-wrap items-center gap-4">
        <input
          type="date"
          value={selectedDate}
          onChange={(e) => setSelectedDate(e.target.value)}
          className="bg-slate-700 text-white px-4 py-2 rounded-lg"
        />
        <button
          onClick={() => setShowPlanModal(true)}
          className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
        >
          Plan Today
        </button>
        <button
          onClick={() => {
            // Clear everything instantly - no confirmation
            const empty = { morning: [], evening: [] };
            localStorage.setItem(`athlete-todays-plan-${selectedDate}`, JSON.stringify(empty));
            localStorage.setItem(`athlete-sessions-${selectedDate}`, JSON.stringify(empty));
            setTodaysPlan(empty);
            setSessions(empty);
            setCoachPlanData(empty);
          }}
          className="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700"
          title="Clear all data for today"
        >
          🗑️ Clear
        </button>
        <button
          onClick={() => setShowWeekPlanModal(true)}
          className="px-4 py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700 flex items-center gap-2"
        >
          <CalendarDays size={20} />
          Plan Week
        </button>
        <button
          onClick={() => {
            setShowCoachPlanModal(true);
            setCoachPlanStep('workflow');
            // Load existing plan if it exists, otherwise start fresh
            if (todaysPlan.morning.length > 0 || todaysPlan.evening.length > 0) {
              setCoachPlanData({ 
                morning: todaysPlan.morning || [], 
                evening: todaysPlan.evening || [] 
              });
            } else {
              setCoachPlanData({ morning: [], evening: [] });
            }
          }}
          className="px-4 py-2 bg-gradient-to-r from-orange-600 to-red-600 text-white rounded-lg hover:from-orange-700 hover:to-red-700 flex items-center gap-2 font-semibold"
        >
          <Dumbbell size={20} />
          Coach's Today Plan
        </button>
        <div className="ml-auto flex gap-2">
          <button
            onClick={() => setShowSummaryModal(true)}
            className="px-3 py-2 bg-gradient-to-r from-green-600 to-emerald-600 text-white rounded-lg hover:from-green-700 hover:to-emerald-700 flex items-center gap-2"
          >
            <TrendingUp size={18} />
            Summary
          </button>
          <button
            onClick={() => {
              try {
                // Export all data
                const allData = {
                  sessions: {},
                  plans: {},
                  version: 'v1',
                  exportDate: new Date().toISOString()
                };
                
                // Get all sessions from localStorage
                for (let i = 0; i < localStorage.length; i++) {
                  const key = localStorage.key(i);
                  if (key && key.startsWith('athlete-sessions-')) {
                    const value = localStorage.getItem(key);
                    if (value) {
                      allData.sessions[key] = JSON.parse(value);
                    }
                  }
                  if (key && key.startsWith('athlete-todays-plan-')) {
                    const value = localStorage.getItem(key);
                    if (value) {
                      allData.plans[key] = JSON.parse(value);
                    }
                  }
                }
                
                const dataStr = JSON.stringify(allData, null, 2);
                setExportData(dataStr);
                setShowExportModal(true);
              } catch (error) {
                alert('Export failed: ' + error.message);
              }
            }}
            className="px-3 py-2 bg-slate-600 text-white rounded-lg hover:bg-slate-500 text-sm"
            title="Export all data"
          >
            📥 Export
          </button>
          <button
            onClick={() => {
              setImportData('');
              setShowImportModal(true);
            }}
            className="px-3 py-2 bg-slate-600 text-white rounded-lg hover:bg-slate-500 text-sm"
            title="Import data"
          >
            📤 Import
          </button>
        </div>
      </div>
    </div>

    {/* Today's Plan Preview */}
    {(todaysPlan.morning.length > 0 || todaysPlan.evening.length > 0) && (
      <div className="bg-slate-800 rounded-xl p-4 mb-4 border-2 border-blue-500/30">
        <h2 className="text-lg font-bold text-white mb-3 flex items-center gap-2">
          <Calendar size={20} className="text-blue-400" />
          Today's Plan
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
          {/* Morning Plan */}
          <div className="bg-slate-700 rounded-lg p-3">
            <div className="flex items-center justify-between mb-2">
              <h3 className="text-white font-semibold flex items-center gap-2">
                <Sun size={18} className="text-yellow-400" />
                Morning Session
                {todaysPlan.morning.length > 0 && (
                  <span className="text-xs bg-yellow-500 text-black px-2 py-0.5 rounded-full">
                    {todaysPlan.morning.length} categories
                  </span>
                )}
              </h3>
              {todaysPlan.morning.length > 0 && (
                <button
                  onClick={() => {
                    const updated = { ...todaysPlan, morning: [] };
                    localStorage.setItem(`athlete-todays-plan-${selectedDate}`, JSON.stringify(updated));
                    setTodaysPlan(updated);
                  }}
                  className="px-2 py-1 bg-red-600 hover:bg-red-700 text-white rounded text-xs"
                  title="Clear morning plan"
                >
                  🗑️
                </button>
              )}
            </div>
            {todaysPlan.morning.length === 0 ? (
              <p className="text-slate-400 text-sm">No plan created</p>
            ) : (
              <div className="space-y-1">
                {todaysPlan.morning.map((cat, idx) => (
                  <div key={idx} className="flex items-center justify-between bg-slate-600 p-2 rounded text-sm">
                    <div className="flex-1">
                      <span className="text-white font-medium">{cat.category}</span>
                      <span className="text-slate-300 text-xs ml-2">({cat.exercises.length} exercises)</span>
                    </div>
                    <button
                      onClick={() => {
                        setEditingSession('morning');
                        setCurrentPlanCategory(cat.category);
                        setShowEditPlanModal(true);
                      }}
                      className="px-2 py-1 bg-blue-600 hover:bg-blue-700 text-white rounded text-xs flex items-center gap-1 ml-2"
                    >
                      <Edit size={12} />
                      Edit
                    </button>
                  </div>
                ))}
              </div>
            )}
          </div>

          {/* Evening Plan */}
          <div className="bg-slate-700 rounded-lg p-3">
            <div className="flex items-center justify-between mb-2">
              <h3 className="text-white font-semibold flex items-center gap-2">
                <Moon size={18} className="text-blue-400" />
                Evening Session
                {todaysPlan.evening.length > 0 && (
                  <span className="text-xs bg-blue-500 text-white px-2 py-0.5 rounded-full">
                    {todaysPlan.evening.length} categories
                  </span>
                )}
              </h3>
              {todaysPlan.evening.length > 0 && (
                <button
                  onClick={() => {
                    const updated = { ...todaysPlan, evening: [] };
                    localStorage.setItem(`athlete-todays-plan-${selectedDate}`, JSON.stringify(updated));
                    setTodaysPlan(updated);
                  }}
                  className="px-2 py-1 bg-red-600 hover:bg-red-700 text-white rounded text-xs"
                  title="Clear evening plan"
                >
                  🗑️
                </button>
              )}
            </div>
            {todaysPlan.evening.length === 0 ? (
              <p className="text-slate-400 text-sm">No plan created</p>
            ) : (
              <div className="space-y-1">
                {todaysPlan.evening.map((cat, idx) => (
                  <div key={idx} className="flex items-center justify-between bg-slate-600 p-2 rounded text-sm">
                    <div className="flex-1">
                      <span className="text-white font-medium">{cat.category}</span>
                      <span className="text-slate-300 text-xs ml-2">({cat.exercises.length} exercises)</span>
                    </div>
                    <button
                      onClick={() => {
                        setEditingSession('evening');
                        setCurrentPlanCategory(cat.category);
                        setShowEditPlanModal(true);
                      }}
                      className="px-2 py-1 bg-blue-600 hover:bg-blue-700 text-white rounded text-xs flex items-center gap-1 ml-2"
                    >
                      <Edit size={12} />
                      Edit
                    </button>
                  </div>
                ))}
              </div>
            )}
          </div>
        </div>
      </div>
    )}

    <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 mb-4">
      <SessionCard sessionType="morning" sessionData={sessions.morning} icon={Sun} />
      <SessionCard sessionType="evening" sessionData={sessions.evening} icon={Moon} />
    </div>

    {showPlanModal && (
      <div className="fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4">
        <div className="bg-slate-800 rounded-2xl max-w-7xl w-full max-h-[95vh] flex flex-col">
          <div className="p-3 border-b border-slate-700">
            <div className="flex items-center justify-between mb-3">
              <div>
                <h2 className="text-lg font-bold text-white">
                  {planningStep === 'groups' && 'Step 1: Select Training Focus'}
                  {planningStep === 'categories' && 'Step 2: Select Categories'}
                  {planningStep === 'exercises' && 'Step 3: Select Exercises'}
                  {planningForDate && (
                    <span className="ml-2 text-sm text-blue-400">
                      ({new Date(planningForDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric', weekday: 'short' })})
                    </span>
                  )}
                </h2>
                <p className="text-xs text-slate-400 mt-0.5">
                  {planningStep === 'groups' && 'Choose one or more training areas'}
                  {planningStep === 'categories' && 'Pick specific categories'}
                  {planningStep === 'exercises' && 'Choose specific exercises'}
                </p>
              </div>
              <button 
                onClick={() => {
                  setShowPlanModal(false);
                  setPlanningCategories({ morning: [], evening: [] });
                  setPlanningStep('groups');
                  setSelectedGroup('');
                  setPlanningSession('morning');
                  // If we were in week planning, go back to week modal
                  if (planningForDate) {
                    setPlanningForDate('');
                    setShowWeekPlanModal(true);
                  }
                }} 
                className="text-white"
              >
                <X size={24} />
              </button>
            </div>
            
            <div className="flex gap-2">
              <button
                onClick={() => setPlanningSession('morning')}
                className={`flex-1 px-4 py-2 rounded-lg font-semibold text-sm flex items-center justify-center gap-2 transition ${
                  planningSession === 'morning'
                    ? 'bg-yellow-600 text-white'
                    : 'bg-slate-700 text-slate-400 hover:bg-slate-600'
                }`}
              >
                <Sun size={18} />
                Morning
              </button>
              <button
                onClick={() => setPlanningSession('evening')}
                className={`flex-1 px-4 py-2 rounded-lg font-semibold text-sm flex items-center justify-center gap-2 transition ${
                  planningSession === 'evening'
                    ? 'bg-blue-600 text-white'
                    : 'bg-slate-700 text-slate-400 hover:bg-slate-600'
                }`}
              >
                <Moon size={18} />
                Evening
              </button>
            </div>
          </div>

          {planningStep === 'groups' && (
            <>
              <div className="p-3 overflow-y-auto flex-1">
                <div className="grid grid-cols-2 md:grid-cols-3 gap-2">
                  {Object.keys(categoryGroups).map((group) => {
                    const isSelected = selectedGroup.split(',').includes(group);
                    
                    return (
                      <button
                        key={group}
                        onClick={() => {
                          const selected = selectedGroup ? selectedGroup.split(',').filter(g => g) : [];
                          if (isSelected) {
                            setSelectedGroup(selected.filter(g => g !== group).join(','));
                          } else {
                            setSelectedGroup([...selected, group].join(','));
                          }
                        }}
                        className={`p-3 rounded-lg text-white transition text-left ${
                          isSelected
                            ? 'bg-gradient-to-br from-green-600 to-green-700'
                            : 'bg-gradient-to-br from-slate-700 to-slate-600 hover:from-blue-600 hover:to-blue-700'
                        }`}
                      >
                        <div className="flex items-center justify-between mb-2">
                          <h3 className="font-bold text-base">{group}</h3>
                          {isSelected && (
                            <div className="bg-white/20 rounded-full p-0.5">
                              <Check size={16} />
                            </div>
                          )}
                        </div>
                        <div className="space-y-0.5">
                          {categoryGroups[group].map((category, idx) => (
                            <div key={idx} className="flex items-center gap-1.5 text-xs opacity-90">
                              <div className="w-1 h-1 rounded-full bg-white/60 flex-shrink-0"></div>
                              <span className="flex-1 truncate">{category}</span>
                              <span className="text-xs opacity-70 bg-white/10 px-1 py-0.5 rounded flex-shrink-0">
                                {exerciseCategoriesWithDefaults[category]?.length || 0}
                              </span>
                            </div>
                          ))}
                        </div>
                      </button>
                    );
                  })}
                </div>
              </div>
              <div className="p-3 border-t border-slate-700 flex gap-3">
                <div className="flex-1 text-slate-300 text-sm flex items-center">
                  {selectedGroup ? selectedGroup.split(',').length : 0} focus areas selected
                </div>
                <button
                  onClick={() => {
                    setShowPlanModal(false);
                    setPlanningCategories({ morning: [], evening: [] });
                    setPlanningStep('groups');
                    setSelectedGroup('');
                    setPlanningSession('morning');
                  }}
                  className="px-4 py-2 bg-slate-600 hover:bg-slate-500 text-white rounded-lg text-sm"
                >
                  Cancel
                </button>
                <button
                  onClick={() => setPlanningStep('categories')}
                  disabled={!selectedGroup}
                  className={`px-4 py-2 rounded-lg text-sm font-semibold ${
                    selectedGroup
                      ? 'bg-blue-600 hover:bg-blue-700 text-white'
                      : 'bg-slate-600 text-slate-400 cursor-not-allowed'
                  }`}
                >
                  Next: Select Categories →
                </button>
              </div>
            </>
          )}

          {planningStep === 'categories' && (
            <>
              <div className="p-3 overflow-y-auto flex-1">
                <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2">
                  {selectedGroup.split(',').flatMap(group => 
                    categoryGroups[group] || []
                  ).map((category) => {
                    if (!exerciseCategoriesWithDefaults[category]) return null;
                    const isSelected = planningCategories[planningSession].some(c => c.category === category);
                    
                    return (
                      <button
                        key={category}
                        onClick={() => {
                          const updated = { ...planningCategories };
                          
                          if (isSelected) {
                            updated[planningSession] = updated[planningSession].filter(c => c.category !== category);
                          } else {
                            updated[planningSession].push({
                              id: `${category}-${Date.now()}`,
                              category: category,
                              exercises: exerciseCategoriesWithDefaults[category].map(ex => ({
                                ...ex,
                                completed: false
                              }))
                            });
                          }
                          
                          setPlanningCategories(updated);
                        }}
                        className={`p-3 rounded-lg transition text-left ${
                          isSelected
                            ? 'bg-green-600 hover:bg-green-700 text-white'
                            : 'bg-slate-700 hover:bg-slate-600 text-white'
                        }`}
                      >
                        <div className="flex items-center justify-between mb-1">
                          <h3 className="font-bold text-sm">{category}</h3>
                          {isSelected && <Check size={16} />}
                        </div>
                        <div className="text-xs opacity-80">
                          {exerciseCategoriesWithDefaults[category].length} exercises
                        </div>
                      </button>
                    );
                  })}
                </div>
              </div>
              <div className="p-3 border-t border-slate-700 flex gap-3">
                <button
                  onClick={() => {
                    setPlanningStep('groups');
                  }}
                  className="px-4 py-2 bg-slate-600 hover:bg-slate-500 text-white rounded-lg text-sm"
                >
                  ← Back to Groups
                </button>
                <div className="flex-1 text-slate-300 text-sm flex items-center">
                  {planningCategories[planningSession].length} categories selected
                </div>
                <button
                  onClick={() => setPlanningStep('exercises')}
                  disabled={planningCategories[planningSession].length === 0}
                  className={`px-4 py-2 rounded-lg text-sm font-semibold ${
                    planningCategories[planningSession].length > 0
                      ? 'bg-blue-600 hover:bg-blue-700 text-white'
                      : 'bg-slate-600 text-slate-400 cursor-not-allowed'
                  }`}
                >
                  Next: Select Exercises →
                </button>
              </div>
            </>
          )}

          {planningStep === 'exercises' && (
            <>
              <div className="p-3 overflow-y-auto flex-1">
                <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2">
                  {planningCategories[planningSession].map((categoryData, catIdx) => {
                    const isExpanded = currentPlanCategory === categoryData.category;
                    const allExercises = exerciseCategoriesWithDefaults[categoryData.category];
                    
                    return (
                      <div key={catIdx} className="bg-slate-700 rounded-lg flex flex-col">
                        <button
                          onClick={() => setCurrentPlanCategory(isExpanded ? '' : categoryData.category)}
                          className="flex items-center justify-between p-2 hover:bg-slate-600 rounded-t-lg transition w-full text-left"
                        >
                          <div className="flex items-center gap-1.5 flex-1 min-w-0">
                            {isExpanded ? <ChevronDown size={16} className="text-blue-400 flex-shrink-0" /> : <ChevronRight size={16} className="text-slate-400 flex-shrink-0" />}
                            <h3 className="font-bold text-white text-sm truncate">{categoryData.category}</h3>
                          </div>
                          <div className="bg-green-500 text-white px-1.5 py-0.5 rounded text-xs flex items-center gap-1 flex-shrink-0 ml-1">
                            {categoryData.exercises.length}/{allExercises.length}
                          </div>
                        </button>
                        
                        {isExpanded && (
                          <div className="p-2 pt-0 flex flex-col">
                            <div className="space-y-1 overflow-y-auto max-h-48 mb-2">
                              {allExercises.map((exercise, idx) => {
                                const isSelected = categoryData.exercises.some(e => e.name === exercise.name);
                                
                                return (
                                  <label
                                    key={idx}
                                    className={`flex items-center gap-1.5 p-1 rounded cursor-pointer text-xs ${
                                      isSelected ? 'bg-blue-600/30' : 'hover:bg-slate-600'
                                    }`}
                                  >
                                    <input
                                      type="checkbox"
                                      checked={isSelected}
                                      onChange={(e) => {
                                        const updated = { ...planningCategories };
                                        
                                        if (e.target.checked) {
                                          updated[planningSession][catIdx].exercises.push({
                                            ...exercise,
                                            completed: false
                                          });
                                        } else {
                                          updated[planningSession][catIdx].exercises = updated[planningSession][catIdx].exercises.filter(
                                            ex => ex.name !== exercise.name
                                          );
                                        }
                                        
                                        setPlanningCategories(updated);
                                      }}
                                      className="w-3 h-3 flex-shrink-0"
                                    />
                                    <span className="text-sm">{exerciseEmojis[exercise.name] || '💪'}</span>
                                    <span className="text-white truncate flex-1">{exercise.name}</span>
                                  </label>
                                );
                              })}
                            </div>
                            
                            <div className="pt-2 border-t border-slate-600 flex gap-1">
                              <button
                                onClick={() => {
                                  const updated = { ...planningCategories };
                                  updated[planningSession][catIdx].exercises = allExercises.map(ex => ({
                                    ...ex,
                                    completed: false
                                  }));
                                  setPlanningCategories(updated);
                                }}
                                className="flex-1 px-2 py-1 bg-blue-600 hover:bg-blue-700 text-white rounded text-xs"
                              >
                                All
                              </button>
                              <button
                                onClick={() => {
                                  const updated = { ...planningCategories };
                                  updated[planningSession][catIdx].exercises = [];
                                  setPlanningCategories(updated);
                                }}
                                className="flex-1 px-2 py-1 bg-slate-600 hover:bg-slate-500 text-white rounded text-xs"
                              >
                                Clear
                              </button>
                            </div>
                          </div>
                        )}
                        
                        {!isExpanded && (
                          <div className="px-2 pb-2">
                            <div className="text-xs text-slate-400">
                              Click to adjust
                            </div>
                          </div>
                        )}
                      </div>
                    );
                  })}
                </div>
              </div>
              <div className="p-3 border-t border-slate-700 flex gap-3">
                <button
                  onClick={() => setPlanningStep('categories')}
                  className="px-4 py-2 bg-slate-600 hover:bg-slate-500 text-white rounded-lg text-sm"
                >
                  ← Back to Categories
                </button>
                <div className="flex-1 text-slate-300 text-sm flex items-center">
                  {planningCategories[planningSession].reduce((sum, cat) => sum + cat.exercises.length, 0)} exercises
                </div>
                <button
                  onClick={() => {
                    const targetDate = planningForDate || selectedDate;
                    const updatedPlan = {
                      ...todaysPlan,
                      [planningSession]: [...planningCategories[planningSession].filter(cat => cat.exercises.length > 0)]
                    };
                    
                    // Save to the specific date
                    try {
                      const key = `athlete-todays-plan-${targetDate}`;
                      localStorage.setItem(key, JSON.stringify(updatedPlan));
                    } catch (error) {
                      console.error('Save plan failed:', error);
                    }
                    
                    // If planning for current date, update state too
                    if (targetDate === selectedDate) {
                      setTodaysPlan(updatedPlan);
                    }
                    
                    setShowPlanModal(false);
                    setPlanningCategories({ morning: [], evening: [] });
                    setPlanningStep('groups');
                    setCurrentPlanCategory('');
                    setSelectedGroup('');
                    setPlanningSession('morning');
                    
                    // If from week planning, go back to week view
                    if (planningForDate) {
                      setPlanningForDate('');
                      setShowWeekPlanModal(true);
                    }
                  }}
                  className="px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg text-sm font-semibold"
                >
                  Save Plan
                </button>
              </div>
            </>
          )}
        </div>
      </div>
    )}

    {/* Plan Week Modal */}
    {showWeekPlanModal && (
      <div className="fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4">
        <div className="bg-slate-800 rounded-2xl max-w-4xl w-full max-h-[90vh] flex flex-col">
          <div className="p-4 border-b border-slate-700 flex items-center justify-between">
            <h2 className="text-xl font-bold text-white flex items-center gap-2">
              <CalendarDays size={24} />
              Plan Your Week
            </h2>
            <button 
              onClick={() => {
                setShowWeekPlanModal(false);
                setPlanningForDate('');
              }} 
              className="text-white"
            >
              <X size={24} />
            </button>
          </div>
          
          <div className="p-6 overflow-y-auto flex-1">
            <p className="text-slate-300 mb-4">Select a day to plan your workout:</p>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
              {getNext7Days().map((date, idx) => {
                const dateObj = new Date(date);
                const dayName = dateObj.toLocaleDateString('en-US', { weekday: 'short' });
                const dayNum = dateObj.getDate();
                const monthName = dateObj.toLocaleDateString('en-US', { month: 'short' });
                const isToday = date === new Date().toISOString().split('T')[0];
                const hasPlan = getDayHasPlan(date);
                
                return (
                  <button
                    key={date}
                    onClick={() => {
                      setPlanningForDate(date);
                      setShowWeekPlanModal(false);
                      setShowPlanModal(true);
                    }}
                    className={`p-4 rounded-xl transition text-center ${
                      isToday
                        ? 'bg-gradient-to-br from-blue-600 to-blue-700 text-white'
                        : hasPlan
                        ? 'bg-gradient-to-br from-green-600 to-green-700 text-white'
                        : 'bg-slate-700 hover:bg-slate-600 text-white'
                    }`}
                  >
                    <div className="text-xs font-semibold opacity-80">{dayName}</div>
                    <div className="text-3xl font-bold my-1">{dayNum}</div>
                    <div className="text-xs opacity-80">{monthName}</div>
                    {hasPlan && (
                      <div className="mt-2 text-xs bg-white/20 rounded px-2 py-1">
                        ✓ Planned
                      </div>
                    )}
                    {isToday && (
                      <div className="mt-2 text-xs bg-white/20 rounded px-2 py-1">
                        Today
                      </div>
                    )}
                  </button>
                );
              })}
            </div>
          </div>
          
          <div className="p-4 border-t border-slate-700">
            <button
              onClick={() => {
                setShowWeekPlanModal(false);
                setPlanningForDate('');
              }}
              className="w-full bg-slate-600 hover:bg-slate-500 text-white py-2 rounded-lg"
            >
              Close
            </button>
          </div>
        </div>
      </div>
    )}

    {/* View Summary Modal - ULTRA COMPACT Single Session */}
    {showSummaryModal && (() => {
      const morningCompleted = [];
      const eveningCompleted = [];
      
      sessions.morning?.forEach((category) => {
        const completed = category.exercises.filter(ex => ex.completed);
        if (completed.length > 0) {
          morningCompleted.push({ category: category.category, exercises: completed });
        }
      });
      
      sessions.evening?.forEach((category) => {
        const completed = category.exercises.filter(ex => ex.completed);
        if (completed.length > 0) {
          eveningCompleted.push({ category: category.category, exercises: completed });
        }
      });
      
      const totalMorning = morningCompleted.reduce((sum, cat) => sum + cat.exercises.length, 0);
      const totalEvening = eveningCompleted.reduce((sum, cat) => sum + cat.exercises.length, 0);
      const totalExercises = totalMorning + totalEvening;
      
      const currentSessionData = summarySession === 'morning' ? morningCompleted : eveningCompleted;
      const currentTotal = summarySession === 'morning' ? totalMorning : totalEvening;
      
      return (
        <div className="fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-1">
          <div className="bg-slate-800 rounded-lg max-w-5xl w-full h-[98vh] flex flex-col overflow-hidden">
            {/* Compact Header with Session Toggle */}
            <div className="px-2 py-1.5 border-b border-slate-700 bg-slate-900">
              <div className="flex items-center justify-between mb-1.5">
                <div className="flex items-center gap-2">
                  <TrendingUp size={16} className="text-green-500" />
                  <h2 className="text-sm font-bold text-white">
                    {new Date(selectedDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}
                  </h2>
                  <span className="bg-blue-600 px-1.5 py-0.5 rounded text-white text-[10px]">{totalExercises} Total</span>
                </div>
                <button onClick={() => {
                  setShowSummaryModal(false);
                  setSummarySession('morning');
                }} className="text-white hover:bg-slate-700 rounded p-1">
                  <X size={16} />
                </button>
              </div>
              
              {/* Session Navigation */}
              <div className="flex gap-2">
                <button
                  onClick={() => setSummarySession('morning')}
                  className={`flex-1 px-3 py-1.5 rounded text-xs font-semibold flex items-center justify-center gap-1 transition ${
                    summarySession === 'morning'
                      ? 'bg-yellow-600 text-white'
                      : 'bg-slate-700 text-slate-300 hover:bg-slate-600'
                  }`}
                >
                  <Sun size={14} />
                  Morning ({totalMorning})
                </button>
                <button
                  onClick={() => setSummarySession('evening')}
                  className={`flex-1 px-3 py-1.5 rounded text-xs font-semibold flex items-center justify-center gap-1 transition ${
                    summarySession === 'evening'
                      ? 'bg-blue-600 text-white'
                      : 'bg-slate-700 text-slate-300 hover:bg-slate-600'
                  }`}
                >
                  <Moon size={14} />
                  Evening ({totalEvening})
                </button>
              </div>
            </div>

            {currentTotal === 0 ? (
              <div className="flex-1 flex items-center justify-center">
                <div className="text-center">
                  <div className="text-4xl mb-2">{summarySession === 'morning' ? '☀️' : '🌙'}</div>
                  <h3 className="text-sm font-bold text-white mb-1">No {summarySession === 'morning' ? 'Morning' : 'Evening'} Exercises</h3>
                  <p className="text-slate-400 text-xs">Complete exercises to see summary</p>
                </div>
              </div>
            ) : (
              <div className="flex-1 overflow-y-auto p-2">
                <div className="space-y-1.5">
                  {currentSessionData.map((cat, idx) => (
                    <div key={idx} className="bg-slate-700/80 rounded p-1.5">
                      <div className="flex items-center gap-0.5 mb-1">
                        <span className="text-sm">{exerciseEmojis[cat.category] || '💪'}</span>
                        <span className="text-[11px] font-bold text-white">{cat.category}</span>
                        <span className="text-[9px] text-slate-400">({cat.exercises.length})</span>
                      </div>
                      <div className="grid grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-0.5">
                        {cat.exercises.map((ex, exIdx) => (
                          <div key={exIdx} className="bg-slate-800 rounded px-1 py-0.5">
                            <div className="flex items-center gap-0.5">
                              <span className="text-[10px]">{exerciseEmojis[ex.name] || '💪'}</span>
                              <span className="text-[9px] text-white truncate flex-1" title={ex.name}>
                                {ex.name.substring(0, 10)}
                              </span>
                              <Check size={8} className="text-green-400 flex-shrink-0" />
                            </div>
                            <div className="text-[8px] text-slate-300 flex gap-0.5 mt-0.5">
                              {ex.sets && <span>S{ex.sets}</span>}
                              {ex.reps && <span>R{ex.reps}</span>}
                              {ex.weight && <span>W{ex.weight}</span>}
                              {ex.duration && <span>D{ex.duration}</span>}
                            </div>
                          </div>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              </div>
            )}
          </div>
        </div>
      );
    })()}

    {/* Export/Import Modal */}
    {showExportModal && (
      <div className="fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4">
        <div className="bg-slate-800 rounded-2xl max-w-4xl w-full max-h-[90vh] flex flex-col">
          <div className="p-4 border-b border-slate-700 flex items-center justify-between">
            <h2 className="text-xl font-bold text-white">📥 Export Data</h2>
            <button onClick={() => setShowExportModal(false)} className="text-white">
              <X size={24} />
            </button>
          </div>
          
          <div className="p-4 overflow-y-auto flex-1">
            <p className="text-slate-300 mb-3">
              Copy this data and save it to a text file. You can import it later by pasting it in the Import box.
            </p>
            <textarea
              value={exportData}
              readOnly
              onClick={(e) => e.target.select()}
              className="w-full h-96 bg-slate-900 text-green-400 p-4 rounded-lg font-mono text-xs border border-slate-600"
              placeholder="Your data will appear here..."
            />
            <div className="mt-3 flex gap-2">
              <button
                onClick={() => {
                  navigator.clipboard.writeText(exportData);
                  alert('Copied to clipboard!');
                }}
                className="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg"
              >
                📋 Copy to Clipboard
              </button>
              <button
                onClick={() => {
                  const blob = new Blob([exportData], { type: 'application/json' });
                  const url = URL.createObjectURL(blob);
                  const a = document.createElement('a');
                  a.href = url;
                  a.download = `athlete-data-${new Date().toISOString().split('T')[0]}.json`;
                  document.body.appendChild(a);
                  a.click();
                  document.body.removeChild(a);
                  URL.revokeObjectURL(url);
                }}
                className="px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg"
              >
                💾 Download File
              </button>
            </div>
          </div>
        </div>
      </div>
    )}

    {/* Import Modal */}
    {showImportModal && (
      <div className="fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4">
        <div className="bg-slate-800 rounded-2xl max-w-4xl w-full max-h-[90vh] flex flex-col">
          <div className="p-4 border-b border-slate-700 flex items-center justify-between">
            <h2 className="text-xl font-bold text-white">📤 Import Data</h2>
            <button onClick={() => setShowImportModal(false)} className="text-white">
              <X size={24} />
            </button>
          </div>
          
          <div className="p-4 overflow-y-auto flex-1">
            <p className="text-slate-300 mb-3">
              Paste your exported data below, or upload a file.
            </p>
            <textarea
              value={importData}
              onChange={(e) => setImportData(e.target.value)}
              className="w-full h-96 bg-slate-900 text-green-400 p-4 rounded-lg font-mono text-xs border border-slate-600"
              placeholder="Paste your JSON data here..."
            />
            <div className="mt-3 flex gap-2">
              <button
                onClick={() => {
                  try {
                    if (!importData.trim()) {
                      alert('Please paste data first!');
                      return;
                    }
                    
                    const data = JSON.parse(importData);
                    
                    // Validate data structure
                    if (!data.sessions && !data.plans) {
                      alert('Invalid data format. Make sure you pasted the exported JSON correctly.');
                      return;
                    }
                    
                    // Import sessions
                    let sessionCount = 0;
                    Object.entries(data.sessions || {}).forEach(([key, value]) => {
                      localStorage.setItem(key, JSON.stringify(value));
                      sessionCount++;
                    });
                    
                    // Import plans
                    let planCount = 0;
                    Object.entries(data.plans || {}).forEach(([key, value]) => {
                      localStorage.setItem(key, JSON.stringify(value));
                      planCount++;
                    });
                    
                    // Close modal and show success
                    setShowImportModal(false);
                    setImportData('');
                    alert(`✅ Success! Imported ${sessionCount} sessions and ${planCount} plans.\n\nPlease refresh the page (F5 or reload button) to see your data.`);
                    
                  } catch (error) {
                    alert('❌ Error importing data: ' + error.message + '\n\nMake sure you copied the complete JSON data.');
                    console.error('Import error:', error);
                  }
                }}
                disabled={!importData.trim()}
                className="px-4 py-2 bg-green-600 hover:bg-green-700 disabled:bg-slate-600 disabled:cursor-not-allowed text-white rounded-lg font-semibold"
              >
                ✓ Import Data
              </button>
              <button
                onClick={() => {
                  const input = document.createElement('input');
                  input.type = 'file';
                  input.accept = '.json,.txt';
                  input.onchange = (e) => {
                    const file = e.target.files[0];
                    if (file) {
                      const reader = new FileReader();
                      reader.onload = (event) => {
                        setImportData(event.target.result);
                      };
                      reader.readAsText(file);
                    }
                  };
                  input.click();
                }}
                className="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg"
              >
                📁 Upload File
              </button>
            </div>
          </div>
        </div>
      </div>
    )}

    {/* Coach's Plan Modal */}
    {showCoachPlanModal && (
      <div className="fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-4">
        <div className="bg-slate-800 rounded-2xl max-w-4xl w-full max-h-[90vh] flex flex-col">
          <div className="p-4 border-b border-slate-700">
            <div className="flex items-center justify-between mb-3">
              <h2 className="text-xl font-bold text-white flex items-center gap-2">
                <Dumbbell size={24} className="text-orange-500" />
                Coach's Today Plan
              </h2>
              <div className="flex items-center gap-2">
                <button
                  onClick={() => {
                    // Reset coach plan data to empty
                    const emptyCoachPlan = { morning: [], evening: [] };
                    setCoachPlanData(emptyCoachPlan);
                    
                    // Also clear from today's plan
                    const emptyPlan = { morning: [], evening: [] };
                    const key = `athlete-todays-plan-${selectedDate}`;
                    localStorage.setItem(key, JSON.stringify(emptyPlan));
                    setTodaysPlan(emptyPlan);
                  }}
                  className="px-3 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg text-sm font-semibold"
                  title="Clear all selections"
                >
                  🗑️ Clear All
                </button>
                <button 
                  onClick={() => {
                    setShowCoachPlanModal(false);
                    setCoachPlanStep('workflow');
                    setCurrentCoachSection('');
                    setCoachPlanSession('morning');
                  }} 
                  className="text-white"
                >
                  <X size={24} />
                </button>
              </div>
            </div>
            
            {/* Session Toggle */}
            <div className="flex gap-2">
              <button
                onClick={() => setCoachPlanSession('morning')}
                className={`flex-1 px-4 py-2 rounded-lg font-semibold flex items-center justify-center gap-2 transition ${
                  coachPlanSession === 'morning'
                    ? 'bg-yellow-600 text-white'
                    : 'bg-slate-700 text-slate-300 hover:bg-slate-600'
                }`}
              >
                <Sun size={20} />
                Morning Session
              </button>
              <button
                onClick={() => setCoachPlanSession('evening')}
                className={`flex-1 px-4 py-2 rounded-lg font-semibold flex items-center justify-center gap-2 transition ${
                  coachPlanSession === 'evening'
                    ? 'bg-blue-600 text-white'
                    : 'bg-slate-700 text-slate-300 hover:bg-slate-600'
                }`}
              >
                <Moon size={20} />
                Evening Session
              </button>
            </div>
          </div>

          {/* Workflow View */}
          {coachPlanStep === 'workflow' && (
            <div className="p-6 overflow-y-auto flex-1">
              <p className="text-slate-300 mb-4">Follow the structured workout flow:</p>
              <div className="space-y-3">
                {[
                  { id: 'warm-up', title: '1. Warm Up', category: 'Warm Up', icon: '🔥' },
                  { id: 'workout', title: '2. Workout', isWorkout: true, icon: '💪' },
                  { id: 'daily-routine', title: '3. Daily Routine', categories: ['Step Ups', 'Wall High Knee'], icon: '📅' },
                  { id: 'core', title: '4. Core', category: 'Core', icon: '🎯' },
                  { id: 'cool-down', title: '5. Cool Down', category: 'Cool Down', icon: '❄️' }
                ].map((section) => {
                  const hasData = section.category 
                    ? coachPlanData[coachPlanSession].some(c => c.category === section.category)
                    : section.categories
                    ? section.categories.every(cat => coachPlanData[coachPlanSession].some(c => c.category === cat))
                    : section.isWorkout && coachPlanData[coachPlanSession].some(c => !['Warm Up', 'Step Ups', 'Wall High Knee', 'Core', 'Cool Down'].includes(c.category));
                  
                  return (
                    <button
                      key={section.id}
                      onClick={() => {
                        setCurrentCoachSection(section.id);
                        // Workout goes to categories, everything else goes to exercises
                        if (section.isWorkout) {
                          setCoachPlanStep('categories');
                        } else {
                          setCoachPlanStep('exercises');
                        }
                      }}
                      className={`w-full p-4 rounded-xl text-left transition ${
                        hasData
                          ? 'bg-gradient-to-r from-green-600 to-green-700'
                          : 'bg-gradient-to-r from-slate-700 to-slate-600 hover:from-blue-600 hover:to-blue-700'
                      }`}
                    >
                      <div className="flex items-center justify-between">
                        <div className="flex items-center gap-3">
                          <span className="text-3xl">{section.icon}</span>
                          <div>
                            <h3 className="text-lg font-bold text-white">{section.title}</h3>
                            <p className="text-sm text-slate-300">
                              {section.category && `${exerciseCategoriesWithDefaults[section.category]?.length || 0} exercises`}
                              {section.categories && `${section.categories.length} categories`}
                              {section.isWorkout && 'Select workout categories'}
                            </p>
                          </div>
                        </div>
                        {hasData && <Check size={24} className="text-white" />}
                      </div>
                    </button>
                  );
                })}
              </div>
            </div>
          )}

          {/* Exercise Selection View */}
          {coachPlanStep === 'exercises' && (() => {
            const sectionConfig = {
              'warm-up': { category: 'Warm Up', title: 'Warm Up' },
              'daily-routine': { categories: ['Step Ups', 'Wall High Knee'], title: 'Daily Routine' },
              'core': { category: 'Core', title: 'Core' },
              'cool-down': { category: 'Cool Down', title: 'Cool Down' }
            }[currentCoachSection];

            const categories = sectionConfig.category ? [sectionConfig.category] : sectionConfig.categories;

            return (
              <>
                <div className="p-4 overflow-y-auto flex-1">
                  <h3 className="text-lg font-bold text-white mb-4">{sectionConfig.title} - Select Exercises</h3>
                  {categories.map((category) => {
                    const allExercises = exerciseCategoriesWithDefaults[category] || [];
                    const selectedExercises = coachPlanData[coachPlanSession].find(c => c.category === category)?.exercises || [];
                    
                    return (
                      <div key={category} className="mb-4">
                        <div className="flex items-center justify-between mb-2">
                          <h4 className="font-semibold text-white">{category}</h4>
                          <div className="flex gap-2">
                            <button
                              onClick={() => {
                                const updated = { ...coachPlanData };
                                const catIndex = updated[coachPlanSession].findIndex(c => c.category === category);
                                if (catIndex >= 0) {
                                  updated[coachPlanSession][catIndex].exercises = allExercises.map(ex => ({ ...ex, completed: false }));
                                } else {
                                  updated[coachPlanSession].push({
                                    id: `${category}-${Date.now()}`,
                                    category,
                                    exercises: allExercises.map(ex => ({ ...ex, completed: false }))
                                  });
                                }
                                setCoachPlanData(updated);
                              }}
                              className="px-3 py-1 bg-blue-600 hover:bg-blue-700 text-white rounded text-sm"
                            >
                              All
                            </button>
                            <button
                              onClick={() => {
                                const updated = { ...coachPlanData };
                                const catIndex = updated[coachPlanSession].findIndex(c => c.category === category);
                                if (catIndex >= 0) {
                                  updated[coachPlanSession].splice(catIndex, 1);
                                }
                                setCoachPlanData(updated);
                              }}
                              className="px-3 py-1 bg-slate-600 hover:bg-slate-500 text-white rounded text-sm"
                            >
                              Clear
                            </button>
                          </div>
                        </div>
                        <div className="grid grid-cols-2 md:grid-cols-3 gap-2">
                          {allExercises.map((exercise, idx) => {
                            const isSelected = selectedExercises.some(e => e.name === exercise.name);
                            return (
                              <label
                                key={idx}
                                className={`flex items-center gap-2 p-2 rounded cursor-pointer ${
                                  isSelected ? 'bg-blue-600/30' : 'bg-slate-700 hover:bg-slate-600'
                                }`}
                              >
                                <input
                                  type="checkbox"
                                  checked={isSelected}
                                  onChange={(e) => {
                                    const updated = { ...coachPlanData };
                                    let catIndex = updated[coachPlanSession].findIndex(c => c.category === category);
                                    
                                    if (catIndex === -1) {
                                      updated[coachPlanSession].push({
                                        id: `${category}-${Date.now()}`,
                                        category,
                                        exercises: []
                                      });
                                      catIndex = updated[coachPlanSession].length - 1;
                                    }
                                    
                                    if (e.target.checked) {
                                      updated[coachPlanSession][catIndex].exercises.push({ ...exercise, completed: false });
                                    } else {
                                      updated[coachPlanSession][catIndex].exercises = updated[coachPlanSession][catIndex].exercises.filter(ex => ex.name !== exercise.name);
                                    }
                                    
                                    setCoachPlanData(updated);
                                  }}
                                  className="w-4 h-4"
                                />
                                <span className="text-sm">{exerciseEmojis[exercise.name] || '💪'}</span>
                                <span className="text-white text-sm flex-1">{exercise.name}</span>
                              </label>
                            );
                          })}
                        </div>
                      </div>
                    );
                  })}
                </div>
                <div className="p-4 border-t border-slate-700 flex gap-3">
                  <button
                    onClick={() => setCoachPlanStep('workflow')}
                    className="px-4 py-2 bg-slate-600 hover:bg-slate-500 text-white rounded-lg"
                  >
                    ← Back to Workflow
                  </button>
                  <div className="flex-1"></div>
                  <button
                    onClick={() => setCoachPlanStep('workflow')}
                    className="px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg"
                  >
                    Done
                  </button>
                </div>
              </>
            );
          })()}

          {/* Category Selection for Workout */}
          {coachPlanStep === 'categories' && (
            <>
              <div className="p-4 overflow-y-auto flex-1">
                <h3 className="text-lg font-bold text-white mb-4">Workout - Select Categories</h3>
                <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-2">
                  {Object.keys(exerciseCategoriesWithDefaults)
                    .filter(cat => !['Warm Up', 'Step Ups', 'Wall High Knee', 'Core', 'Cool Down'].includes(cat))
                    .map((category) => {
                      const isSelected = coachPlanData[coachPlanSession].some(c => c.category === category);
                      return (
                        <button
                          key={category}
                          onClick={() => {
                            const updated = { ...coachPlanData };
                            if (isSelected) {
                              updated[coachPlanSession] = updated[coachPlanSession].filter(c => c.category !== category);
                            } else {
                              updated[coachPlanSession].push({
                                id: `${category}-${Date.now()}`,
                                category,
                                exercises: exerciseCategoriesWithDefaults[category].map(ex => ({ ...ex, completed: false }))
                              });
                            }
                            setCoachPlanData(updated);
                          }}
                          className={`p-3 rounded-lg text-left transition ${
                            isSelected ? 'bg-green-600 text-white' : 'bg-slate-700 hover:bg-slate-600 text-white'
                          }`}
                        >
                          <div className="flex items-center justify-between mb-1">
                            <span className="font-semibold text-sm">{category}</span>
                            {isSelected && <Check size={16} />}
                          </div>
                          <div className="text-xs opacity-80">
                            {exerciseCategoriesWithDefaults[category].length} exercises
                          </div>
                        </button>
                      );
                    })}
                </div>
              </div>
              <div className="p-4 border-t border-slate-700 flex gap-3">
                <button
                  onClick={() => setCoachPlanStep('workflow')}
                  className="px-4 py-2 bg-slate-600 hover:bg-slate-500 text-white rounded-lg"
                >
                  ← Back to Workflow
                </button>
                <div className="flex-1"></div>
                <button
                  onClick={() => setCoachPlanStep('workflow')}
                  className="px-4 py-2 bg-green-600 hover:bg-green-700 text-white rounded-lg"
                >
                  Done
                </button>
              </div>
            </>
          )}

          {/* Footer - Save Plan */}
          {coachPlanStep === 'workflow' && (
            <div className="p-4 border-t border-slate-700">
              <button
                onClick={() => {
                  // Sort each session's plan in correct order
                  const orderedPlan = { morning: [], evening: [] };
                  const order = ['Warm Up', 'workout', 'Step Ups', 'Wall High Knee', 'Core', 'Cool Down'];
                  
                  ['morning', 'evening'].forEach(session => {
                    order.forEach(item => {
                      if (item === 'workout') {
                        // Add all workout categories
                        coachPlanData[session]
                          .filter(c => !['Warm Up', 'Step Ups', 'Wall High Knee', 'Core', 'Cool Down'].includes(c.category))
                          .forEach(c => orderedPlan[session].push(c));
                      } else {
                        const cat = coachPlanData[session].find(c => c.category === item);
                        if (cat) orderedPlan[session].push(cat);
                      }
                    });
                  });
                  
                  saveTodaysPlan(orderedPlan);
                  setShowCoachPlanModal(false);
                  setCoachPlanStep('workflow');
                  setCurrentCoachSection('');
                  setCoachPlanSession('morning');
                }}
                disabled={coachPlanData.morning.length === 0 && coachPlanData.evening.length === 0}
                className={`w-full py-3 rounded-lg font-semibold ${
                  coachPlanData.morning.length > 0 || coachPlanData.evening.length > 0
                    ? 'bg-gradient-to-r from-orange-600 to-red-600 hover:from-orange-700 hover:to-red-700 text-white'
                    : 'bg-slate-600 text-slate-400 cursor-not-allowed'
                }`}
              >
                Save Coach's Plan
              </button>
            </div>
          )}
        </div>
      </div>
    )}

    {/* Workout Mode */}
    {showWorkoutMode && (() => {
      const allExercises = getAllExercisesInSession(workoutSession);
      const currentEx = allExercises[currentExerciseIndex];
      const progress = ((currentExerciseIndex + 1) / allExercises.length) * 100;
      
      if (!currentEx) {
        return (
          <div className="fixed inset-0 bg-slate-900 z-50 flex items-center justify-center p-4">
            <div className="bg-slate-800 rounded-2xl p-8 max-w-md w-full text-center">
              <div className="text-6xl mb-4">🎉</div>
              <h2 className="text-2xl font-bold text-white mb-4">Workout Complete!</h2>
              <p className="text-slate-300 mb-6">
                Great job! You completed {allExercises.length} exercises.
              </p>
              <button
                onClick={() => {
                  setShowWorkoutMode(false);
                  setCurrentExerciseIndex(0);
                  setWorkoutSession('');
                }}
                className="w-full px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-lg font-semibold"
              >
                Back to Dashboard
              </button>
            </div>
          </div>
        );
      }
      
      return (
        <div className="fixed inset-0 bg-slate-900 z-50 flex flex-col">
          {/* Header */}
          <div className="bg-slate-800 p-4 border-b border-slate-700">
            <div className="flex items-center justify-between mb-2">
              <h2 className="text-xl font-bold text-white flex items-center gap-2">
                {workoutSession === 'morning' ? <Sun size={24} className="text-yellow-400" /> : <Moon size={24} className="text-blue-400" />}
                {workoutSession === 'morning' ? 'Morning' : 'Evening'} Workout
              </h2>
              <button
                onClick={() => {
                  if (confirm('Are you sure you want to exit the workout?')) {
                    setShowWorkoutMode(false);
                    setCurrentExerciseIndex(0);
                    setWorkoutSession('');
                  }
                }}
                className="text-white"
              >
                <X size={24} />
              </button>
            </div>
            <div className="flex items-center justify-between mb-2">
              <div className="text-sm text-slate-400">
                Exercise {currentExerciseIndex + 1} of {allExercises.length}
              </div>
              <div className="flex gap-1 text-xs">
                <span className="bg-green-600 px-2 py-0.5 rounded">
                  ✓ {allExercises.filter(ex => ex.completed || ex.status === 'completed').length}
                </span>
                <span className="bg-orange-600 px-2 py-0.5 rounded">
                  ⏭️ {allExercises.filter(ex => ex.status === 'skipped').length}
                </span>
              </div>
            </div>
            <div className="mt-2 bg-slate-700 rounded-full h-2">
              <div 
                className="bg-green-500 h-2 rounded-full transition-all duration-300"
                style={{ width: `${progress}%` }}
              ></div>
            </div>
          </div>

          {/* Exercise Content */}
          <div className="flex-1 overflow-y-auto p-6">
            <div className="w-full">
              {/* Category Badge */}
              <div className="mb-4">
                <span className="bg-blue-600 text-white px-3 py-1 rounded-full text-sm">
                  {currentEx.categoryName}
                </span>
              </div>

              {/* Exercise Name */}
              <div className="flex items-center gap-4 mb-4">
                <span className="text-5xl">{exerciseEmojis[currentEx.name] || '💪'}</span>
                <div className="flex-1">
                  <h1 className="text-3xl font-bold text-white mb-2">{currentEx.name}</h1>
                  {(() => {
                    const { personalBest, seasonBest } = calculateBests(currentEx.name);
                    if (personalBest > 0 || seasonBest > 0) {
                      return (
                        <div className="flex gap-3 text-sm">
                          {personalBest > 0 && (
                            <div className="bg-yellow-600/20 border border-yellow-600/50 px-3 py-1 rounded-lg">
                              <span className="text-yellow-400 font-bold">PB:</span>
                              <span className="text-white font-bold ml-1">{personalBest}kg</span>
                            </div>
                          )}
                          {seasonBest > 0 && (
                            <div className="bg-blue-600/20 border border-blue-600/50 px-3 py-1 rounded-lg">
                              <span className="text-blue-400 font-bold">SB:</span>
                              <span className="text-white font-bold ml-1">{seasonBest}kg</span>
                              <span className="text-slate-400 text-xs ml-1">({seasonMonths}mo)</span>
                            </div>
                          )}
                        </div>
                      );
                    }
                    return null;
                  })()}
                </div>
              </div>

              {/* Exercise Details - Conditional Entry Method */}
              <div className="bg-slate-800 rounded-lg p-3 mb-3" key={`exercise-${currentExerciseIndex}`}>
                {currentEx.name === 'Half Squads' ? (
                  // SET-BY-SET ENTRY FOR HALF SQUADS
                  <>
                    {/* Auto-parse button */}
                    {currentEx.notes && (
                      <div className="mb-3 p-2 bg-slate-700 rounded">
                        <div className="text-xs text-slate-400 mb-1">Notes: {currentEx.notes}</div>
                        <button
                          onClick={() => {
                            const notes = currentEx.notes || '';
                            const updated = { ...sessions };
                            const exercise = updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex];
                            
                            // Parse reps: look for patterns like "10+8+6" or "12+10+8"
                            const repsMatch = notes.match(/(\d+(?:\+\d+)+)/);
                            const reps = repsMatch ? repsMatch[1].split('+').map(r => r.trim()) : [];
                            
                            // Parse weights: look for patterns like "50+60+70" or "weight: 50+60+70"
                            const weightMatch = notes.match(/(?:weight[s]?:\s*)?(\d+(?:\+\d+)+)\s*(?:kg)?/i);
                            const weights = weightMatch ? weightMatch[1].split('+').map(w => w.trim()) : [];
                            
                            // Set number of sets based on max length
                            const numSets = Math.max(reps.length, weights.length);
                            
                            if (numSets > 0) {
                              exercise.numSets = numSets;
                              exercise.setData = [];
                              
                              for (let i = 0; i < numSets; i++) {
                                exercise.setData.push({
                                  reps: reps[i] || '',
                                  weight: weights[i] || ''
                                });
                              }
                              
                              setSessions(updated);
                              saveSessions(updated);
                              alert(`Loaded ${numSets} sets from notes!`);
                            } else {
                              alert('Could not parse notes. Use format: "10+8+6, weight: 50+60+70kg"');
                            }
                          }}
                          className="px-3 py-1 bg-green-600 hover:bg-green-700 text-white rounded text-sm font-semibold"
                        >
                          📋 Load from Notes
                        </button>
                      </div>
                    )}
                    
                    <div className="mb-4">
                      <label className="text-slate-400 text-sm block mb-2 font-bold">Number of Sets</label>
                      <div className="flex gap-2 flex-wrap">
                        {[1, 2, 3, 4, 5, 6].map(num => (
                          <button
                            key={num}
                            onClick={() => {
                              const updated = { ...sessions };
                              const exercise = updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex];
                              
                              if (!exercise.setData) exercise.setData = [];
                              
                              if (num > exercise.setData.length) {
                                for (let i = exercise.setData.length; i < num; i++) {
                                  exercise.setData.push({ reps: '', weight: '' });
                                }
                              } else {
                                exercise.setData = exercise.setData.slice(0, num);
                              }
                              
                              exercise.numSets = num;
                              setSessions(updated);
                              saveSessions(updated);
                            }}
                            className={`px-4 py-2 rounded-lg font-bold ${
                              (currentEx.numSets || 0) === num ? 'bg-blue-600 text-white' : 'bg-slate-700 text-slate-300 hover:bg-slate-600'
                            }`}
                          >
                            {num}
                          </button>
                        ))}
                      </div>
                    </div>

                    {currentEx.numSets > 0 && (
                      <div className="space-y-2 mb-4">
                        {Array.from({ length: currentEx.numSets || 0 }, (_, setIndex) => {
                          const setData = (currentEx.setData || [])[setIndex] || { reps: '', weight: '' };
                          
                          return (
                            <div key={setIndex} className="bg-slate-700 rounded-lg p-3">
                              <div className="flex items-center gap-3">
                                <div className="text-white font-bold w-16">Set {setIndex + 1}</div>
                                <div className="flex-1 grid grid-cols-2 gap-3">
                                  <div>
                                    <label className="text-slate-400 text-xs block mb-1">Reps</label>
                                    <input
                                      type="number"
                                      value={setData.reps}
                                      onChange={(e) => {
                                        const updated = { ...sessions };
                                        const exercise = updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex];
                                        if (!exercise.setData) exercise.setData = [];
                                        if (!exercise.setData[setIndex]) exercise.setData[setIndex] = {};
                                        exercise.setData[setIndex].reps = e.target.value;
                                        setSessions(updated);
                                        saveSessions(updated);
                                      }}
                                      className="w-full bg-slate-600 text-white px-3 py-2 rounded text-center font-bold border border-slate-500 focus:border-blue-500 focus:outline-none"
                                      placeholder="0"
                                    />
                                  </div>
                                  <div>
                                    <label className="text-slate-400 text-xs block mb-1">Weight (kg)</label>
                                    <input
                                      type="number"
                                      value={setData.weight}
                                      onChange={(e) => {
                                        const updated = { ...sessions };
                                        const exercise = updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex];
                                        if (!exercise.setData) exercise.setData = [];
                                        if (!exercise.setData[setIndex]) exercise.setData[setIndex] = {};
                                        exercise.setData[setIndex].weight = e.target.value;
                                        setSessions(updated);
                                        saveSessions(updated);
                                      }}
                                      className="w-full bg-slate-600 text-white px-3 py-2 rounded text-center font-bold border border-slate-500 focus:border-blue-500 focus:outline-none"
                                      placeholder="0"
                                    />
                                  </div>
                                </div>
                              </div>
                            </div>
                          );
                        })}
                      </div>
                    )}
                  </>
                ) : (
                  // DIAL WHEELS FOR ALL OTHER EXERCISES
                <div className="grid grid-cols-4 gap-2">
                  {/* Sets Wheel */}
                  <div>
                    <label className="text-white text-lg block mb-1 text-center font-bold">Sets</label>
                    <div className="relative h-40 bg-gradient-to-b from-slate-800 via-slate-700 to-slate-800 rounded-xl overflow-hidden w-full">
                      <div className="absolute inset-0 flex items-center justify-center pointer-events-none z-10">
                        <div className="w-full h-12 bg-blue-500/10 border-y border-blue-500/30"></div>
                      </div>
                      <div 
                        className="h-full w-full overflow-y-scroll scrollbar-hide"
                        onScroll={(e) => {
                          const scrollTop = e.target.scrollTop;
                          const itemHeight = 48;
                          const index = Math.round(scrollTop / itemHeight);
                          const updated = { ...sessions };
                          updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex].sets = index.toString();
                          setSessions(updated);
                          saveSessions(updated);
                        }}
                        ref={(el) => {
                          if (el) {
                            const currentValue = parseInt(currentEx.sets) || 0;
                            el.scrollTop = currentValue * 48;
                          }
                        }}
                      >
                        <div className="h-16"></div>
                        {Array.from({ length: 51 }, (_, i) => {
                          const currentScroll = parseInt(currentEx.sets) || 0;
                          const distance = Math.abs(i - currentScroll);
                          const opacity = distance === 0 ? 1 : distance === 1 ? 0.5 : 0.2;
                          const fontSize = distance === 0 ? 'text-4xl' : distance === 1 ? 'text-2xl' : 'text-lg';
                          
                          return (
                            <div 
                              key={i} 
                              className={`h-12 flex items-center justify-center font-bold text-white transition-all ${fontSize}`}
                              style={{ opacity }}
                            >
                              {i}
                            </div>
                          );
                        })}
                        <div className="h-16"></div>
                      </div>
                    </div>
                  </div>

                  {/* Reps Wheel */}
                  <div>
                    <label className="text-white text-lg block mb-1 text-center font-bold">Reps</label>
                    <div className="relative h-40 bg-gradient-to-b from-slate-800 via-slate-700 to-slate-800 rounded-xl overflow-hidden">
                      <div className="absolute inset-0 flex items-center justify-center pointer-events-none z-10">
                        <div className="w-full h-12 bg-blue-500/10 border-y border-blue-500/30"></div>
                      </div>
                      <div 
                        className="h-full w-full overflow-y-scroll scrollbar-hide"
                        onScroll={(e) => {
                          const scrollTop = e.target.scrollTop;
                          const itemHeight = 48;
                          const index = Math.round(scrollTop / itemHeight);
                          const updated = { ...sessions };
                          updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex].reps = index.toString();
                          setSessions(updated);
                          saveSessions(updated);
                        }}
                        ref={(el) => {
                          if (el) {
                            const currentValue = parseInt(currentEx.reps) || 0;
                            el.scrollTop = currentValue * 48;
                          }
                        }}
                      >
                        <div className="h-16"></div>
                        {Array.from({ length: 101 }, (_, i) => {
                          const currentScroll = parseInt(currentEx.reps) || 0;
                          const distance = Math.abs(i - currentScroll);
                          const opacity = distance === 0 ? 1 : distance === 1 ? 0.5 : 0.2;
                          const fontSize = distance === 0 ? 'text-4xl' : distance === 1 ? 'text-2xl' : 'text-lg';
                          
                          return (
                            <div 
                              key={i} 
                              className={`h-12 flex items-center justify-center font-bold text-white transition-all ${fontSize}`}
                              style={{ opacity }}
                            >
                              {i}
                            </div>
                          );
                        })}
                        <div className="h-16"></div>
                      </div>
                    </div>
                  </div>

                  {/* Weight Wheel */}
                  <div>
                    <label className="text-white text-lg block mb-1 text-center font-bold">Weight</label>
                    <div className="relative h-40 bg-gradient-to-b from-slate-800 via-slate-700 to-slate-800 rounded-xl overflow-hidden">
                      <div className="absolute inset-0 flex items-center justify-center pointer-events-none z-10">
                        <div className="w-full h-12 bg-blue-500/10 border-y border-blue-500/30 flex items-center justify-end pr-2">
                          <span className="text-blue-400 text-sm font-medium">kg</span>
                        </div>
                      </div>
                      <div 
                        className="h-full w-full overflow-y-scroll scrollbar-hide"
                        onScroll={(e) => {
                          const scrollTop = e.target.scrollTop;
                          const itemHeight = 48;
                          const index = Math.round(scrollTop / itemHeight);
                          const updated = { ...sessions };
                          updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex].weight = (index * 5).toString();
                          setSessions(updated);
                          saveSessions(updated);
                        }}
                        ref={(el) => {
                          if (el) {
                            const currentValue = parseInt(currentEx.weight) || 0;
                            el.scrollTop = (currentValue / 5) * 48;
                          }
                        }}
                      >
                        <div className="h-16"></div>
                        {Array.from({ length: 61 }, (_, i) => {
                          const currentScroll = Math.round((parseInt(currentEx.weight) || 0) / 5);
                          const distance = Math.abs(i - currentScroll);
                          const opacity = distance === 0 ? 1 : distance === 1 ? 0.5 : 0.2;
                          const fontSize = distance === 0 ? 'text-4xl' : distance === 1 ? 'text-2xl' : 'text-lg';
                          
                          return (
                            <div 
                              key={i} 
                              className={`h-12 flex items-center justify-center font-bold text-white transition-all ${fontSize}`}
                              style={{ opacity }}
                            >
                              {i * 5}
                            </div>
                          );
                        })}
                        <div className="h-16"></div>
                      </div>
                    </div>
                  </div>

                  {/* Duration Wheel */}
                  <div>
                    <label className="text-white text-lg block mb-1 text-center font-bold">Duration</label>
                    <div className="relative h-40 bg-gradient-to-b from-slate-800 via-slate-700 to-slate-800 rounded-xl overflow-hidden">
                      <div className="absolute inset-0 flex items-center justify-center pointer-events-none z-10">
                        <div className="w-full h-12 bg-blue-500/10 border-y border-blue-500/30 flex items-center justify-end pr-2">
                          <span className="text-blue-400 text-sm font-medium">min</span>
                        </div>
                      </div>
                      <div 
                        className="h-full w-full overflow-y-scroll scrollbar-hide"
                        onScroll={(e) => {
                          const scrollTop = e.target.scrollTop;
                          const itemHeight = 48;
                          const index = Math.round(scrollTop / itemHeight);
                          const updated = { ...sessions };
                          updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex].duration = index.toString();
                          setSessions(updated);
                          saveSessions(updated);
                        }}
                        ref={(el) => {
                          if (el) {
                            const currentValue = parseInt(currentEx.duration) || 0;
                            el.scrollTop = currentValue * 48;
                          }
                        }}
                      >
                        <div className="h-16"></div>
                        {Array.from({ length: 121 }, (_, i) => {
                          const currentScroll = parseInt(currentEx.duration) || 0;
                          const distance = Math.abs(i - currentScroll);
                          const opacity = distance === 0 ? 1 : distance === 1 ? 0.5 : 0.2;
                          const fontSize = distance === 0 ? 'text-4xl' : distance === 1 ? 'text-2xl' : 'text-lg';
                          
                          return (
                            <div 
                              key={i} 
                              className={`h-12 flex items-center justify-center font-bold text-white transition-all ${fontSize}`}
                              style={{ opacity }}
                            >
                              {i}
                            </div>
                          );
                        })}
                        <div className="h-16"></div>
                      </div>
                    </div>
                  </div>
                </div>
                )}
                <div className="mt-3">
                  <label className="text-slate-400 text-xs block mb-1 font-bold uppercase">Notes</label>
                  <input
                    type="text"
                    value={currentEx.notes || ''}
                    onChange={(e) => {
                      const updated = { ...sessions };
                      updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex].notes = e.target.value;
                      setSessions(updated);
                      saveSessions(updated);
                    }}
                    className="w-full bg-slate-700 text-white px-3 py-2 rounded-lg text-sm border border-slate-600 focus:border-blue-500 focus:outline-none"
                    placeholder="Add notes..."
                  />
                </div>
              </div>
              
              <style jsx>{`
                .scrollbar-hide::-webkit-scrollbar {
                  display: none;
                }
                .scrollbar-hide {
                  -ms-overflow-style: none;
                  scrollbar-width: none;
                }
              `}</style>
            </div>
          </div>

          {/* Navigation Footer */}
          <div className="bg-slate-800 p-4 border-t border-slate-700">
            <div className="max-w-4xl mx-auto">
              <div className="flex gap-2">
                <button
                  onClick={() => setCurrentExerciseIndex(Math.max(0, currentExerciseIndex - 1))}
                  disabled={currentExerciseIndex === 0}
                  className="px-3 py-3 bg-slate-700 hover:bg-slate-600 disabled:opacity-50 disabled:cursor-not-allowed text-white rounded-lg font-semibold flex items-center gap-1 text-sm"
                >
                  <ChevronLeft size={16} />
                  Prev
                </button>
                <button
                  onClick={() => {
                    // Mark as skipped
                    const updated = { ...sessions };
                    updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex].status = 'skipped';
                    setSessions(updated);
                    saveSessions(updated);
                    setCurrentExerciseIndex(currentExerciseIndex + 1);
                  }}
                  className={`flex-1 px-4 py-3 rounded-lg font-semibold text-sm ${
                    currentEx.status === 'skipped'
                      ? 'bg-orange-600 hover:bg-orange-700'
                      : 'bg-yellow-600 hover:bg-yellow-700'
                  } text-white`}
                >
                  {currentEx.status === 'skipped' ? '⏭️ Skipped' : 'Skip'}
                </button>
                <button
                  onClick={() => {
                    // Mark as completed
                    const updated = { ...sessions };
                    updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex].completed = true;
                    updated[workoutSession][currentEx.categoryIndex].exercises[currentEx.exerciseIndex].status = 'completed';
                    setSessions(updated);
                    saveSessions(updated);
                    setCurrentExerciseIndex(currentExerciseIndex + 1);
                  }}
                  className={`flex-1 px-4 py-3 rounded-lg font-semibold text-sm flex items-center justify-center gap-1 ${
                    currentEx.completed || currentEx.status === 'completed'
                      ? 'bg-emerald-600 hover:bg-emerald-700'
                      : 'bg-green-600 hover:bg-green-700'
                  } text-white`}
                >
                  <Check size={16} />
                  {currentEx.completed || currentEx.status === 'completed' ? 'Completed' : 'Complete'}
                </button>
                <button
                  onClick={() => {
                    setCurrentExerciseIndex(currentExerciseIndex + 1);
                  }}
                  disabled={currentExerciseIndex >= allExercises.length - 1}
                  className="px-3 py-3 bg-slate-700 hover:bg-slate-600 disabled:opacity-50 disabled:cursor-not-allowed text-white rounded-lg font-semibold flex items-center gap-1 text-sm"
                >
                  Next
                  <ChevronLeft size={16} className="rotate-180" />
                </button>
              </div>
            </div>
          </div>
        </div>
      );
    })()}

    {/* Edit Plan Modal */}
    {showEditPlanModal && (
      <div className="fixed inset-0 bg-black/70 flex items-center justify-center z-50 p-2">
        <div className="bg-slate-800 rounded-xl max-w-7xl w-full max-h-[98vh] flex flex-col">
          <div className="p-2 border-b border-slate-700 flex items-center justify-between">
            <h2 className="text-base font-bold text-white flex items-center gap-2">
              <Edit size={18} />
              {currentPlanCategory}
            </h2>
            <button 
              onClick={() => {
                setShowEditPlanModal(false);
                setEditingSession('');
                setCurrentPlanCategory('');
              }} 
              className="text-white"
            >
              <X size={20} />
            </button>
          </div>
          
          <div className="p-2 overflow-y-auto flex-1">
            {todaysPlan[editingSession]
              ?.filter(cat => cat.category === currentPlanCategory)
              .map((category, catIdx) => {
                const actualCatIdx = todaysPlan[editingSession].findIndex(c => c.category === currentPlanCategory);
                
                return (
                  <div key={catIdx} className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-2">
                    {category.exercises.map((exercise, exIdx) => (
                      <div key={exIdx} className="bg-slate-700 rounded p-2 border border-slate-600">
                        <div className="flex items-center gap-1 mb-2 pb-1 border-b border-slate-600">
                          <span className="text-base">{exerciseEmojis[exercise.name] || '💪'}</span>
                          <span className="text-white font-semibold text-xs flex-1 truncate" title={exercise.name}>{exercise.name}</span>
                        </div>
                        <div className="space-y-1">
                          <div className="grid grid-cols-2 gap-1">
                            <div>
                              <label className="text-[10px] text-slate-400 block">Sets</label>
                              <input
                                type="text"
                                value={exercise.sets || ''}
                                onChange={(e) => {
                                  const updated = { ...todaysPlan };
                                  updated[editingSession][actualCatIdx].exercises[exIdx].sets = e.target.value;
                                  saveTodaysPlan(updated);
                                }}
                                className="w-full bg-slate-600 text-white px-1 py-1 rounded text-xs border border-slate-500 focus:border-blue-500 focus:outline-none"
                                placeholder="3"
                              />
                            </div>
                            <div>
                              <label className="text-[10px] text-slate-400 block">Reps</label>
                              <input
                                type="text"
                                value={exercise.reps || ''}
                                onChange={(e) => {
                                  const updated = { ...todaysPlan };
                                  updated[editingSession][actualCatIdx].exercises[exIdx].reps = e.target.value;
                                  saveTodaysPlan(updated);
                                }}
                                className="w-full bg-slate-600 text-white px-1 py-1 rounded text-xs border border-slate-500 focus:border-blue-500 focus:outline-none"
                                placeholder="10"
                              />
                            </div>
                          </div>
                          <div className="grid grid-cols-2 gap-1">
                            <div>
                              <label className="text-[10px] text-slate-400 block">Kg</label>
                              <input
                                type="text"
                                value={exercise.weight || ''}
                                onChange={(e) => {
                                  const updated = { ...todaysPlan };
                                  updated[editingSession][actualCatIdx].exercises[exIdx].weight = e.target.value;
                                  saveTodaysPlan(updated);
                                }}
                                className="w-full bg-slate-600 text-white px-1 py-1 rounded text-xs border border-slate-500 focus:border-blue-500 focus:outline-none"
                                placeholder="50"
                              />
                            </div>
                            <div>
                              <label className="text-[10px] text-slate-400 block">Min</label>
                              <input
                                type="text"
                                value={exercise.duration || ''}
                                onChange={(e) => {
                                  const updated = { ...todaysPlan };
                                  updated[editingSession][actualCatIdx].exercises[exIdx].duration = e.target.value;
                                  saveTodaysPlan(updated);
                                }}
                                className="w-full bg-slate-600 text-white px-1 py-1 rounded text-xs border border-slate-500 focus:border-blue-500 focus:outline-none"
                                placeholder="5"
                              />
                            </div>
                          </div>
                          <div>
                            <label className="text-[10px] text-slate-400 block">Note</label>
                            <input
                              type="text"
                              value={exercise.notes || ''}
                              onChange={(e) => {
                                const updated = { ...todaysPlan };
                                updated[editingSession][actualCatIdx].exercises[exIdx].notes = e.target.value;
                                saveTodaysPlan(updated);
                              }}
                              className="w-full bg-slate-600 text-white px-1 py-1 rounded text-xs border border-slate-500 focus:border-blue-500 focus:outline-none"
                              placeholder="Notes..."
                            />
                          </div>
                        </div>
                      </div>
                    ))}
                  </div>
                );
              })}
          </div>
          
          <div className="p-2 border-t border-slate-700">
            <button
              onClick={() => {
                setShowEditPlanModal(false);
                setEditingSession('');
                setCurrentPlanCategory('');
              }}
              className="w-full bg-green-600 hover:bg-green-700 text-white py-2 rounded-lg font-semibold text-sm"
            >
              ✓ Done
            </button>
          </div>
        </div>
      </div>
    )}
  </div>
</div>
```

);
}

```
    // Render the app
    const root = ReactDOM.createRoot(document.getElementById('root'));
    root.render(<AthleteDashboard />);
</script>

<!-- Service Worker Registration -->
<script>
    if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
            navigator.serviceWorker.register('/service-worker.js')
                .then(reg => console.log('✅ Service Worker registered'))
                .catch(err => console.log('❌ SW registration failed:', err));
        });
    }
</script>
```

</body>
</html>